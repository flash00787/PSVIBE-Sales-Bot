BUG INVESTIGATION: PS VIBE Sales Bot - Food Stock Inventory + Sale Daily Food Menu Error

==== ROOT CAUSE ANALYSIS ====

1. FOOD STOCK INVENTORY DATA INCONSISTENCY
   - Multiple data sources creating synchronization issues:
     * API endpoints (priority): api_fetch_food_prices_async() -> MySQL
     * Fallback: Google Sheets (setting_sh columns 4,5) 
     * Real-time stock: _replit_get("stock/current") -> API server inventory
   
   - Stock sync flow broken:
     * stock_in.py updates Google Sheets directly (deprecated path)
     * update_inv_total_k1() reads from different source (food_prices vs stock/current)
     * food_stock_map built from _replit_get("stock/current") API
     * Prices fetched from different source (Google Sheets cols 4,5)

2. SALE DAILY FOOD MENU STEP ERRORS
   - step_food_menu() (line 756): Filters food_prices by stock_map availability
   - Empty food_prices after stock filtering causes graceful fallback to main menu
   - KeyError risks in step_food_qty() when last_food not in prices dict

==== KEY FINDINGS ====

BUG 2 - Food Stock Inventory Data Incorrect:
✓ CONFIRMED: Data source mismatch between stock_in updates and food menu reads
  - stock_in.py writes to Google Sheets (deprecated fallback)
  - sales.py reads stock from API server _replit_get("stock/current")
  - Different timestamp/sync between sheet writes and API reads

✓ update_inv_total_k1() ISSUES:
  - Only calculates value from prices×costs, doesn't update actual stock quantities
  - Missing connection between stock-in records and live inventory API
  - Function marked DEPRECATED but still called after stock operations

BUG 3 - Sale Daily Food Menu Step Error:
✓ SAFETY MECHANISM WORKING: prompt_food_menu() rebuilds stock_map if missing
✓ GRACEFUL DEGRADATION: Returns to main menu if no items after stock filtering
✓ POTENTIAL KEYERROR: step_food_qty() accesses context.user_data["last_food"] without validation

==== CRITICAL CODE SECTIONS ====

1. PROMPT_FOOD_MENU (lines 202-270):
   - Rebuilds stock_map from _replit_get("stock/current") if missing/empty
   - Filters food_prices to only show items with stock > 0
   - Returns to main menu if no items available after filtering

2. STOCK_IN.PY full analysis:
   - Uses deprecated direct gspread writes to Google Sheets
   - Calls update_inv_total_k1() after recording stock-in
   - API write attempts with fallback to sheets (best-effort)
   - stock_in_sh.append_row() for each cart item

3. STOCK_OUT during sale (lines 1250-1290):
   - Records to both API (best-effort) and Google Sheets
   - Calls update_inv_total_k1() after food sales
   - Cache refresh: _replit_get("stock/current?nocache=1")

==== DATA FLOW PROBLEMS ====

Stock-In Flow:
stock_in.py → Google Sheets → update_inv_total_k1() → (no API sync)

Sales Flow:
sales.py → _replit_get("stock/current") → API server inventory

Mismatch: Stock-in writes to sheets, sales reads from API server!

==== RECOMMENDED FIXES ====

1. IMMEDIATE: Add KeyError protection in step_food_qty():
   ```python
   name = context.user_data.get("last_food")
   if not name:
       return await prompt_food_menu(update, context)
   ```

2. SYNC FIX: Ensure stock_in.py API writes succeed:
   - Make API writes mandatory, not best-effort
   - Add retry mechanism for failed API stock-in writes
   - Remove deprecated Google Sheets fallback

3. DATA SOURCE CONSISTENCY:
   - Migrate all stock operations to use same API endpoints
   - Remove direct gspread dependencies in stock operations
   - Centralize stock management through single API layer

==== SERVICES STATUS CHECK ====

Before applying fixes, need to verify:
- psvibe-api.service status (handles stock/current endpoint)
- MySQL connection health
- API authentication working

==== FILES TO MODIFY ====

Priority fixes needed in:
1. /root/psvibe-sales-bot/bot/handlers/sales.py (step_food_qty KeyError protection)
2. /root/psvibe-sales-bot/bot/handlers/stock_in.py (mandatory API writes)
3. /root/psvibe-sales-bot/bot/handlers/stock.py (update_inv_total_k1 API migration)

=== RESULT: OK ===