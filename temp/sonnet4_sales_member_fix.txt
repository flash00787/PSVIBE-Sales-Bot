=== PS VIBE Sales Bot Deep Audit — Critical State Management Bugs ===

CRITICAL FINDINGS:

=== BUG: CONFLICTING STATE ID NUMBERS ===
File: /root/psvibe-sales-bot/bot/__init__.py
Lines: ~1040-1041 & ~1047 
Code: 
```python
CON_EDIT_MULT_SELECT = 101
FINANCE_MENU = 100
OPEX_CAT = 101
```
Root Cause: TWO different states have the same ID number (101) — CON_EDIT_MULT_SELECT and OPEX_CAT both = 101. This creates a collision where only one handler will work.
Fix: Renumber one of them to avoid conflict. Should be:
```python
OPEX_CAT = 103  # Change from 101 to 103
```

=== BUG: MISSING step_ FUNCTIONS IN sales.py ===
File: /root/psvibe-sales-bot/bot/handlers/sales.py
Lines: Throughout file
Code: Multiple step functions called but not defined
Root Cause: The sales.py file imports functions like `step_member()`, `step_console()` but they are NOT defined in this module. App.py registers handlers for these states but the actual functions don't exist.
Fix: Add the missing step functions:
```python
async def step_member(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Handle member selection logic
    
async def step_console(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Handle console selection logic
    
async def step_mins(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Handle minutes selection logic
```

=== BUG: APP.PY STATE REGISTRATION WITHOUT FUNCTIONS ===
File: /root/psvibe-sales-bot/bot/app.py
Lines: ~158-172 (Daily Sales flow section)
Code:
```python
states={
    MEMBER:          [MessageHandler(filters.TEXT & ~filters.COMMAND, step_member)],
    CONSOLE:         [MessageHandler(filters.TEXT & ~filters.COMMAND, step_console)],
    MINS:            [MessageHandler(filters.TEXT & ~filters.COMMAND, step_mins)],
}
```
Root Cause: App.py references step_member, step_console, step_mins functions but these are not imported or defined anywhere.
Fix: Either import these functions from sales.py or define them locally in app.py

=== BUG: INCONSISTENT RETURN TYPES IN members.py ===
File: /root/psvibe-sales-bot/bot/handlers/members.py  
Lines: Multiple functions throughout
Code: Several step functions return wrong BotState values or None
Root Cause: Functions like `step_mm_menu()` and `step_tu_member()` sometimes return async function calls instead of proper BotState integers.
Fix: Ensure all step functions return proper BotState values:
```python
return MAIN_MENU  # Not: return await show_main_menu(...)
```

=== BUG: MISSING STATE HANDLERS IN APP.PY ===  
File: /root/psvibe-sales-bot/bot/app.py
Lines: States dict missing several BotState enum values
Code: States like NM_STAFF, STAFF_SELECT are referenced in members.py but not in app.py states dict
Root Cause: BotState enum defines states that are not registered in ConversationHandler
Fix: Add missing state handlers to app.py states dict

=== BUG: CIRCULAR IMPORT RISK ===
File: /root/psvibe-sales-bot/bot/handlers/sales.py & members.py
Lines: Import statements at top
Code: Both files import from bot module which imports from these handlers
Root Cause: Potential circular import between bot/__init__.py and handler modules
Fix: Move state definitions to separate constants module

SUMMARY:
- 1 CRITICAL state ID collision (101) 
- Multiple missing step function definitions
- Inconsistent return values from state handlers  
- Missing state registrations in ConversationHandler
- Potential circular import issues

PRIORITY FIX ORDER:
1. Fix state ID collision (101) first - BLOCKS ALL flows
2. Add missing step_member/step_console/step_mins functions  
3. Fix return value consistency in members.py
4. Add missing state handlers to app.py
5. Resolve circular imports

=== RESULT: OK |||