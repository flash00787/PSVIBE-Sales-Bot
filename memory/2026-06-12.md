# 2026-06-12 Daily Log

## SSH Access — ISP DPI Blocking
- All direct SSH ports (22, 80, 443, 22222) from Boss's Windows timeout
- Kora container can SSH to VPS fine (localhost)
- **Conclusion:** ISP doing Deep Packet Inspection (DPI) blocking SSH protocol regardless of port
- **Solutions tried:**
  - Added SSH on port 80 (failed — DPI detects SSH protocol)
  - Installed wssh (web SSH terminal) on port 8888
  - Added Cloudflare Tunnel ingress for `shell.ps-vibe.com → localhost:8888`
  - `cloudflared access ssh` failed with "websocket: bad handshake" (needs Zero Trust config)
- **DNS needed:** `shell.ps-vibe.com` CNAME → tunnel still needs Public Hostname config in Zero Trust dashboard
- **Boss provided:** Cloudflare API Token (`cfut_2su...`) but server IP masked by WARP
- **Status:** On hold — Boss to configure Cloudflare Zero Trust → Tunnels → Public Hostnames when ready

## Game Library Fix (Customer Bot)
### MySQL Game Titles — 23 corrected in `games_library`
- Fixed misspelled titles at source (e.g., "Elder Ring" → "Elden Ring", "Sprit Fiction" → "Split Fiction")
- Ran MySQL UPDATE directly on `psvibe_api.games_library`

### console_games Table — 68 rows updated
- After fixing `games_library`, Boss reported console data "not reading"
- Found `console_games` table still had OLD misspelled game titles
- Updated 68 rows to match corrected names

### Customer Bot Code Fixes
- **FAQ intercept fix:** Moved `_match_faq()` check AFTER button routing in `handle_menu_buttons()` (was intercepting "Game Library" button with score ≥2)
- **"Not Installed" filter removed:** `_is_shown_game()` cleaned — all 41 games now show
- **AI prompt game names fixed:** `_build_live_game_library_text()` uses canonical name from `TITLE_ALIASES`
- **`text` variable bug fixed:** `handlers.py` FAQ check before text assignment fixed

## Booking Check-in — "Member Format" Fix
- **Issue:** Booking customer session-end triggers wallet balance check → shows "Wallet insufficient" (Top Up/Cash Down/Skip) screen
- **Root cause:** `launch_session_sale()` in `sales.py` checks `wallet_balance` for ALL non-guest members, even booking customers (who have 0 wallet balance)
- **Fix:** Added `if booking_id:` check before wallet check — booking sessions skip wallet check and go directly to sales voucher
- **File:** `/root/psvibe-sales-bot/bot/handlers/sales.py` — inserted at line 1677
- **Sales Bot** restarted successfully

## Services Status (End of Session)
| Service | Status |
|---------|--------|
| psvibe-api | ✅ Active |
| psvibe-sale-bot | ✅ Active |
| psvibe_customer_bot | ✅ Active |
| cloudflared-tunnel | ✅ Active |
| wssh (web SSH) | ✅ Running on :8888 |

## Pending Items
1. **Shell.ps-vibe.com** — Boss needs to add Public Hostname in Cloudflare Zero Trust → Tunnels
2. **CMD SSH** — Blocked by ISP DPI; only Cloudflare Tunnel or VPN can bypass
# 2026-06-12 Daily Log

## SSH Access — ISP DPI Blocking
- All direct SSH ports (22, 80, 443, 22222) from Boss's Windows timeout
- Kora container can SSH to VPS fine (localhost)
- **Conclusion:** ISP doing Deep Packet Inspection (DPI) blocking SSH protocol regardless of port
- **Solutions tried:**
  - Added SSH on port 80 (failed — DPI detects SSH protocol)
  - Installed wssh (web SSH terminal) on port 8888
  - Added Cloudflare Tunnel ingress for `shell.ps-vibe.com → localhost:8888`
  - `cloudflared access ssh` failed with "websocket: bad handshake" (needs Zero Trust config)
- **DNS needed:** `shell.ps-vibe.com` CNAME → tunnel still needs Public Hostname config in Zero Trust dashboard
- **Boss provided:** Cloudflare API Token (`cfut_2su...`) but server IP masked by WARP
- **Status:** On hold — Boss to configure Cloudflare Zero Trust → Tunnels → Public Hostnames when ready

## Game Library Fix (Customer Bot)
### MySQL Game Titles — 23 corrected in `games_library`
- Fixed misspelled titles at source (e.g., "Elder Ring" → "Elden Ring", "Sprit Fiction" → "Split Fiction")
- Ran MySQL UPDATE directly on `psvibe_api.games_library`

### console_games Table — 68 rows updated
- After fixing `games_library`, Boss reported console data "not reading"
- Found `console_games` table still had OLD misspelled game titles
- Updated 68 rows to match corrected names

### Customer Bot Code Fixes
- **FAQ intercept fix:** Moved `_match_faq()` check AFTER button routing in `handle_menu_buttons()` (was intercepting "Game Library" button with score ≥2)
- **"Not Installed" filter removed:** `_is_shown_game()` cleaned — all 41 games now show
- **AI prompt game names fixed:** `_build_live_game_library_text()` uses canonical name from `TITLE_ALIASES`
- **`text` variable bug fixed:** `handlers.py` FAQ check before text assignment fixed

## Dashboard Games Library Fix (+ Added)
- **Issue:** Dashboard's Games section showed wrong install status + install location
- **Root cause analysis:** `final_status` column in `games_library` had wrong/obsolete values:
  - Many games showing "Not Installed" but actually installed on consoles (console_games)
  - `final_status` format was inconsistent (console IDs like "C-01, C-04", "Installed", "Not Installed", "Available") — Dashboard only supports: `Available`, `Damaged`, `Lost`, `In Use`
  - God of War Ragnarök had encoding issue ("Ã¶" vs "ö") — matched via `LIKE %Ragnar%`
- **Fix (sync_game_status.py → fix_games_library_status.py):**
  1. Queried console_games for all actually-installed games
  2. Updated 41 games_library rows to correct Dashboard values:
     - Games on any console → `In Use`
     - Games not on any console → `Available`
  3. Also fixed `FC 26 disc_count = 10 → 1` (was clearly wrong)
  4. God of War Ragnarök → `In Use` ✅ (found via LIKE matching)
- **Dashboard format confirmed:** JS code at `/dashboard-dist/assets/GamesLibrary-CVkz9uC8.js` shows only 4 valid statuses: Available, Damaged, Lost, In Use

## Booking Check-in — "Member Format" Fix
- **Issue:** Booking customer session-end triggers wallet balance check → shows "Wallet insufficient" (Top Up/Cash Down/Skip) screen
- **Root cause:** `launch_session_sale()` in `sales.py` checks `wallet_balance` for ALL non-guest members, even booking customers (who have 0 wallet balance)
- **Fix:** Added `if booking_id:` check before wallet check — booking sessions skip wallet check and go directly to sales voucher
- **File:** `/root/psvibe-sales-bot/bot/handlers/sales.py` — inserted at line 1684
- **Sales Bot** restarted successfully

## Dashboard API Structure (Discovered)
- Dashboard frontend: SPA at `dashboard-dist/` (Vue/React bundle)
- API routes: `/root/psvibe_api_server/dashboard_routes.py`
  - `/games` → queries `games_library` (title, genre, solo_multi, final_status, disc_count)
  - `/sales` → daily/hourly sales
  - `/food-stock` → food inventory
  - `/console-booking` → booking schedule
- Dashboard runs on port 9090 via `dashboard.service` (uvicorn)

## Services Status (End of Session)
| Service | Status |
|---------|--------|
| psvibe-api | ✅ Active |
| psvibe-sale-bot | ✅ Active |
| psvibe_customer_bot | ✅ Active |
| psvibe-dashboard | ✅ Active (:9090) |
| cloudflared-tunnel | ✅ Active |
| wssh (web SSH) | ✅ Running on :8888 |
| kora-dashboard | ✅ Active (:9091) |

## Pending Items
1. **Shell.ps-vibe.com** — Boss needs to add Public Hostname in Cloudflare Zero Trust → Tunnels
2. **CMD SSH** — Blocked by ISP DPI; only Cloudflare Tunnel or VPN can bypass
3. **God of War Ragnarök encoding** — Only handled via LIKE; if library column encoding is fixed, console_games may need separate update

## Key Technical Notes
- **Dashboard only supports 4 statuses** — `final_status` must be: Available, Damaged, Lost, In Use
- **games_library vs console_games** — Two independent tables; names must match between both for queries to work
- **Cloudflare API Token** — Token valid but server's WARP IP (104.28.163.33), not matching token IP restriction
# 2026-06-12 Daily Log

## SSH Access — ISP DPI Blocking
- All direct SSH ports (22, 80, 443, 22222) from Boss's Windows timeout
- Kora container can SSH to VPS fine (localhost)
- **Conclusion:** ISP doing Deep Packet Inspection (DPI) blocking SSH protocol regardless of port
- **Solutions tried:**
  - Added SSH on port 80 (failed — DPI detects SSH protocol)
  - Installed wssh (web SSH terminal) on port 8888
  - Added Cloudflare Tunnel ingress for `shell.ps-vibe.com → localhost:8888`
  - `cloudflared access ssh` failed with "websocket: bad handshake" (needs Zero Trust config)
- **DNS needed:** `shell.ps-vibe.com` CNAME → tunnel still needs Public Hostname config in Zero Trust dashboard
- **Boss provided:** Cloudflare API Token (`cfut_2su...`) but server IP masked by WARP
- **Status:** On hold — Boss to configure Cloudflare Zero Trust → Tunnels → Public Hostnames when ready

## Game Library Fix (Customer Bot)
### MySQL Game Titles — 23 corrected in `games_library`
- Fixed misspelled titles at source (e.g., "Elder Ring" → "Elden Ring", "Sprit Fiction" → "Split Fiction")
- Ran MySQL UPDATE directly on `psvibe_api.games_library`

### console_games Table — 68 rows updated
- After fixing `games_library`, Boss reported console data "not reading"
- Found `console_games` table still had OLD misspelled game titles
- Updated 68 rows to match corrected names

### Customer Bot Code Fixes
- **FAQ intercept fix:** Moved `_match_faq()` check AFTER button routing in `handle_menu_buttons()` (was intercepting "Game Library" button with score ≥2)
- **"Not Installed" filter removed:** `_is_shown_game()` cleaned — all 41 games now show
- **AI prompt game names fixed:** `_build_live_game_library_text()` uses canonical name from `TITLE_ALIASES`
- **`text` variable bug fixed:** `handlers.py` FAQ check before text assignment fixed

## Dashboard Games Library Fix
- **Issue:** Dashboard's Games section showed wrong install status + install location
- **Root cause:** `final_status` column in `games_library` had wrong/obsolete values (console IDs like "C-01, C-04", "Installed", "Not Installed") — Dashboard JS only supports: `Available`, `Damaged`, `Lost`, `In Use`
  - God of War Ragnarök had encoding issue ("Ã¶" vs "ö") — matched via `LIKE %Ragnar%`
- **Fix:** `fix_games_library_status.py` updated 41 rows to Dashboard-compatible values:
  - Games on any console → `In Use`
  - Games not on any console → `Available`
  - God of War Ragnarök → `In Use` ✅ (matched via LIKE)

## Booking Check-in — Wallet Skip
- **Issue:** Booking customer session-end triggers wallet balance check → shows "Wallet insufficient" screen
- **Fix:** Added `if booking_id:` guard in `launch_session_sale()` (sales.py line 1677) to skip wallet check for booking customers
- **Sales Bot** restarted successfully

## FC 26 disc_count — Reverted to 10
- Earlier fix changed FC 26 disc_count=10 → 1 (assumed typo)
- **Boss explicitly said:** Do NOT modify disc counts. FC 26 disc_count=10 is correct.
- Reverted back to 10.

## Low Stock Alert — Frequency Reduced
- Previous: `*/30 * * * *` (every 30 min → spam in staff group)
- New: `0 */4 * * *` (every 4 hours)
- Daily summary at 9 AM MMT kept as-is
- File: `/var/spool/cron/crontabs/root`

## Kora Smart-Reminder — Thread Fixed
- **Issue:** Reminders going to wrong topic/thread in staff group
- **Root cause:** `threadId: "125454"` (wrong thread for current conversation)
- **Fix:** Changed thread ID to `"125192"` (current active thread). Added `bestEffort: true`.

## Session End Timer — Fixed (message_thread_id)

**Root cause:** `_remind_loop` called `bot.send_message()` without `message_thread_id`. If staff group has Forum/topics mode, messages without thread ID go to General topic where staff may miss them.

**Fix applied to 3 files:**
1. `booking_flow.py` — `_remind_loop` accepts `message_thread_id` param, passes to `send_message`
2. `session_reminder_store.py` — `persist_reminder` stores `message_thread_id`, `restore_reminders_async` passes it back
3. `booking.py` — passes `message_thread_id` from `update.effective_message` when creating reminder task
4. `booking_flow.py` (`cb_extend_timer`) — passes `message_thread_id` from callback query message when extending

**Syntax verified:** All 3 files pass `py_compile`. Bot restarted at 07:44 UTC.

## Session End Timer System
- **System:** `_remind_loop` in `booking_flow.py` (sales bot)
- Persisted to: `/root/psvibe-sales-bot/data/session_reminders.json`
- Restored on boot via `restore_reminders_async()`
- Target: `STAFF_NOTIFY_CHAT` = `-1003686032747` (staff group)
- Uses `message_thread_id` from booking for Forum mode support
- **Status:** Restored C-01 timer at 06:38 UTC with 2972s delay → first fire ~07:28 UTC

### Session End Timer — Reminder Delivery Issue
- **Boss reports:** Reminders not arriving in staff group
- **Investigation:** Staff group may have Forum/topics mode enabled
  - Kora sent test message to group and it arrived ✅ (group not dead)
  - Sales bot's `_remind_loop` may need `message_thread_id` if group has Forum mode
  - If reminders go to General topic without proper thread, staff may not see them
  - **Next check:** Monitor ~07:28 UTC timer fire. If no arrival, check if `message_thread_id` needed in `_remind_loop`

## Booking Game Selection — Missing Games (`[:30]` Limit)
- **Issue:** Osmo reported game selection screen missing some games during session start
- **Root cause:** `booking.py` has `games_list[:30]` limit — last 11 games (Tekken 8, The Witcher 3, Split Fiction, etc.) are cut off
- **Memo for future fix:** Remove or increase the `[:30]` slice in booking.py game selection handler

## Dashboard API Structure
- Dashboard frontend: SPA at `dashboard-dist/` (Vue/React bundle)
- API routes: `/root/psvibe_api_server/dashboard_routes.py`
  - `/games` → queries `games_library` (title, genre, solo_multi, final_status, disc_count)
  - `/sales` → daily/hourly sales
  - `/food-stock` → food inventory
  - `/console-booking` → booking schedule
- Dashboard runs on port 9090 via `dashboard.service` (uvicorn)

## Services Status (End of Session)
| Service | Status |
|---------|--------|
| psvibe-api | ✅ Active |
| psvibe-sale-bot | ✅ Active |
| psvibe_customer_bot | ✅ Active |
| psvibe-dashboard | ✅ Active (:9090) |
| cloudflared-tunnel | ✅ Active |
| wssh (web SSH) | ✅ Running on :8888 |
| kora-dashboard | ✅ Active (:9091) |
| Caddy (nginx proxy) | ✅ Active |
| n8n | ✅ Active |
| MySQL | ✅ Active |
| Health Monitor | 93.3 / 100 ✅ |

## Pending Items
1. **Shell.ps-vibe.com** — Boss needs to add Public Hostname in Cloudflare Zero Trust → Tunnels
2. **CMD SSH** — Blocked by ISP DPI; only Cloudflare Tunnel or VPN can bypass
3. **God of War Ragnarök encoding** — Only handled via LIKE; clean fix needed if encoding corrected
4. **Booking game selection `[:30]` limit** — Remove/increase limit so all 41 games show
5. **Session end timer reminder thread** — Verify after ~07:28 UTC fire; may need message_thread_id for Forum topics
6. **n8n payment (€25.68)** — 2nd notice received, payment pending
7. **GitHub Deploy failing** — PSVIBE-API-Server master branch deploy workflow failing

## Key Technical Notes
- **Dashboard only supports 4 statuses** — `final_status` must be: Available, Damaged, Lost, In Use
- **games_library vs console_games** — Two independent tables; names must match between both for queries to work
- **Cloudflare API Token** — Token valid but server's WARP IP (104.28.163.33), not matching token IP restriction
- **Timezone reminder:** ALL timestamps UTC unless noted. Boss is UTC+6:30 (Myanmar)
- **Can't modify disc_count** — Boss explicitly prohibited; FC 26 disc_count=10 is intentional

## Files Modified (June 12 session)
- `/root/psvibe-sales-bot/customer_bot/handlers.py` — FAQ intercept fix, text var fix
- `/root/psvibe-sales-bot/customer_bot/api.py` — Game library data mapping
- `/root/psvibe-sales-bot/customer_bot/games.py` — AI prompt names fix
- `/root/psvibe-sales-bot/bot/handlers/sales.py` — Booking wallet skip (line 1684)
- `/root/psvibe-sales-bot/bot/handlers/booking_flow.py` — _remind_loop restored, _cancel_remind added
- `/root/psvibe-sales-bot/bot/handlers/session_reminder_store.py` — Persistence system
- `/root/psvibe_api_server/dashboard_routes.py` — Console booking endpoint
- `/etc/ssh/sshd_config` — Port 22 uncommented, port 80 added for SSH testing
- `/etc/cloudflared/config.yml` — shell.ps-vibe.com ingress, protocol http2
- `/var/spool/cron/crontabs/root` — Low stock alert reduced to 4h
- MySQL: `games_library` (41 rows), `console_games` (68 rows) — titles corrected
- `/home/node/.openclaw/workspace/smart_reminder.js` — Thread ID fixed to 125192

## What Kora Learned
1. **Dashboard JS only supports 4 statuses** — future frontend changes must stay within Available/Damaged/Lost/In Use
2. **SSH over non-standard port still detected by ISP DPI** — SSH protocol handshake is identifiable regardless of port
3. **SQL game titles must match exactly across tables** — games_library and console_games need identical names
4. **Booking customers should skip wallet check** — they pay at booking time, not at session-end
5. **disc_count values are intentional** — don't "fix" them without explicit Boss confirmation
6. **Cron interval changes need patience** — check after 1 cycle to confirm before marking done
7. **Staff group Forum mode** — if group has topics, messages without message_thread_id go to General topic where staff may miss them
# 2026-06-12 Daily Log

## SSH Access — ISP DPI Blocking
- All direct SSH ports (22, 80, 443, 22222) from Boss's Windows timeout
- Kora container can SSH to VPS fine (localhost)
- **Conclusion:** ISP doing Deep Packet Inspection (DPI) blocking SSH protocol regardless of port
- **Solutions tried:**
  - Added SSH on port 80 (failed — DPI detects SSH protocol)
  - Installed wssh (web SSH terminal) on port 8888
  - Added Cloudflare Tunnel ingress for `shell.ps-vibe.com → localhost:8888`
  - `cloudflared access ssh` failed with "websocket: bad handshake" (needs Zero Trust config)
- **DNS needed:** `shell.ps-vibe.com` CNAME → tunnel still needs Public Hostname config in Zero Trust dashboard
- **Boss provided:** Cloudflare API Token (`cfut_2su...`) but server IP masked by WARP
- **Status:** On hold — Boss to configure Cloudflare Zero Trust → Tunnels → Public Hostnames when ready

## Game Library Fix (Customer Bot)
### MySQL Game Titles — 23 corrected in `games_library`
- Fixed misspelled titles at source (e.g., "Elder Ring" → "Elden Ring", "Sprit Fiction" → "Split Fiction")
- Ran MySQL UPDATE directly on `psvibe_api.games_library`

### console_games Table — 68 rows updated
- After fixing `games_library`, Boss reported console data "not reading"
- Found `console_games` table still had OLD misspelled game titles
- Updated 68 rows to match corrected names

### Customer Bot Code Fixes
- **FAQ intercept fix:** Moved `_match_faq()` check AFTER button routing in `handle_menu_buttons()` (was intercepting "Game Library" button with score ≥2)
- **"Not Installed" filter removed:** `_is_shown_game()` cleaned — all 41 games now show
- **AI prompt game names fixed:** `_build_live_game_library_text()` uses canonical name from `TITLE_ALIASES`
- **`text` variable bug fixed:** `handlers.py` FAQ check before text assignment fixed

## Dashboard Games Library Fix
- **Issue:** Dashboard's Games section showed wrong install status + install location
- **Root cause:** `final_status` column in `games_library` had wrong/obsolete values (console IDs like "C-01, C-04", "Installed", "Not Installed") — Dashboard JS only supports: `Available`, `Damaged`, `Lost`, `In Use`
  - God of War Ragnarök had encoding issue ("Ã¶" vs "ö") — matched via `LIKE %Ragnar%`
- **Fix:** `fix_games_library_status.py` updated 41 rows to Dashboard-compatible values:
  - Games on any console → `In Use`
  - Games not on any console → `Available`
  - God of War Ragnarök → `In Use` ✅ (matched via LIKE)

## Booking Check-in — Wallet Skip
- **Issue:** Booking customer session-end triggers wallet balance check → shows "Wallet insufficient" screen
- **Fix:** Added `if booking_id:` guard in `launch_session_sale()` (sales.py line 1677) to skip wallet check for booking customers
- **Sales Bot** restarted successfully

## FC 26 disc_count — Reverted to 10
- Earlier fix changed FC 26 disc_count=10 → 1 (assumed typo)
- **Boss explicitly said:** Do NOT modify disc counts. FC 26 disc_count=10 is correct.
- Reverted back to 10.

## Low Stock Alert — Frequency Reduced
- Previous: `*/30 * * * *` (every 30 min → spam in staff group)
- New: `0 */4 * * *` (every 4 hours)
- Daily summary at 9 AM MMT kept as-is
- File: `/var/spool/cron/crontabs/root`

## Kora Smart-Reminder — Thread Fixed
- **Issue:** Reminders going to wrong topic/thread in staff group
- **Root cause:** `threadId: "125454"` (wrong thread for current conversation)
- **Fix:** Changed thread ID to `"125192"` (current active thread). Added `bestEffort: true`.

## Session End Timer — Fixed (message_thread_id)

**Root cause:** `_remind_loop` called `bot.send_message()` without `message_thread_id`. If staff group has Forum/topics mode, messages without thread ID go to General topic where staff may miss them.

**Fix applied to 3 files:**
1. `booking_flow.py` — `_remind_loop` accepts `message_thread_id` param, passes to `send_message`
2. `session_reminder_store.py` — `persist_reminder` stores `message_thread_id`, `restore_reminders_async` passes it back
3. `booking.py` — passes `message_thread_id` from `update.effective_message` when creating reminder task
4. `booking_flow.py` (`cb_extend_timer`) — passes `message_thread_id` from callback query message when extending

**Syntax verified:** All 3 files pass `py_compile`. Bot restarted at 07:44 UTC.

## Session End Timer System
- **System:** `_remind_loop` in `booking_flow.py` (sales bot)
- Persisted to: `/root/psvibe-sales-bot/data/session_reminders.json`
- Restored on boot via `restore_reminders_async()`
- Target: `STAFF_NOTIFY_CHAT` = `-1003686032747` (staff group)
- Uses `message_thread_id` from booking for Forum mode support
- **Status:** Restored C-01 timer at 06:38 UTC with 2972s delay → first fire ~07:28 UTC

### Session End Timer — Reminder Delivery Issue
- **Boss reports:** Reminders not arriving in staff group
- **Investigation:** Staff group may have Forum/topics mode enabled
  - Kora sent test message to group and it arrived ✅ (group not dead)
  - Sales bot's `_remind_loop` may need `message_thread_id` if group has Forum mode
  - If reminders go to General topic without proper thread, staff may not see them
  - **Next check:** Monitor ~07:28 UTC timer fire. If no arrival, check if `message_thread_id` needed in `_remind_loop`

## Booking Game Selection — Missing Games (`[:30]` Limit)
- **Issue:** Osmo reported game selection screen missing some games during session start
- **Root cause:** `booking.py` has `games_list[:30]` limit — last 11 games (Tekken 8, The Witcher 3, Split Fiction, etc.) are cut off
- **Memo for future fix:** Remove or increase the `[:30]` slice in booking.py game selection handler

## Dashboard API Structure
- Dashboard frontend: SPA at `dashboard-dist/` (Vue/React bundle)
- API routes: `/root/psvibe_api_server/dashboard_routes.py`
  - `/games` → queries `games_library` (title, genre, solo_multi, final_status, disc_count)
  - `/sales` → daily/hourly sales
  - `/food-stock` → food inventory
  - `/console-booking` → booking schedule
- Dashboard runs on port 9090 via `dashboard.service` (uvicorn)

## Web SSH — `ps-vibe.com/shell/` (Confirmed Working)
- **WebSSH (wssh)** installed on port 8888, systemd service active
- **API proxy** added at `/root/psvibe_api_server/app.py` lines 3021+ — `/shell/` route proxies to ws://localhost:8888
- **Verified:** `localhost:8000/shell` → 200, `https://ps-vibe.com/shell/` → 200
- **Boss tested and got through** — Web SSH works via browser
- **Limitation:** Boss wants CMD SSH, not web-based
- **cloudflared access tcp** suggested as workaround for CMD-based SSH

## SSH: IP Completely Unreachable from Boss's Internet
- **New finding:** `ping 5.223.81.16` → all packets lost (100% packet loss, Request timed out)
- **Not just port blocking** — entire IP unreachable from Boss's connection
- Kora can still reach the VPS from OpenClaw container (different network path)
- **Root cause:** Myanmar ISP / routing issue to Hetzner Germany (not DPI alone)
- **Workaround options offered:**
  1. `cloudflared access tcp --hostname ps-vibe.com --url tcp://localhost:2222` then `ssh root@localhost -p 2222`
  2. VPN (mobile hotspot suggested)
  3. Web SSH via `https://ps-vibe.com/shell/`
- **Boss prefers CMD solution over Web SSH**

## FAQ Auto-Reply — Disabled
- Boss requested FAQ temporarily disabled due to wrong game info (100+ games claim vs 41 actual)
- `handlers.py` lines 856-860 (FAQ block) commented out
- Customer bot restarted at ~07:59 UTC
- Will re-enable after game count discrepancy is resolved

## 100+ Games Discrepancy
- `games_library` has only 41 games
- `console_games` has 68+ records (games installed across 10 consoles)
- Boss says there should be 100+ games from old GSheet
- Possible: old GSheet included hardware/accessory rows counted as "games"
- Games may have been lost during GSheet → DB import
- **Pending:** Boss needs to clarify if games were truly lost or original count was inflated by accessories

## Services Status (End of Session)
| Service | Status |
|---------|--------|
| psvibe-api | ✅ Active |
| psvibe-sale-bot | ✅ Active |
| psvibe_customer_bot | ✅ Active |
| psvibe-dashboard | ✅ Active (:9090) |
| cloudflared-tunnel | ✅ Active |
| wssh (web SSH) | ✅ Running on :8888 |
| kora-dashboard | ✅ Active (:9091) |
| Caddy (nginx proxy) | ✅ Active |
| n8n | ✅ Active |
| MySQL | ✅ Active |
| Health Monitor | 93.3 / 100 ✅ |

## Pending Items
1. **CMD SSH** — IP 5.223.81.16 completely unreachable from Boss's Myanmar ISP. Solutions: cloudflared access tcp, VPN, or direct IP fix
2. **God of War Ragnarök encoding** — Only handled via LIKE; clean fix needed if encoding corrected
3. **Booking game selection `[:30]` limit** — Remove/increase limit so all 41 games show
4. **Session end timer reminder thread** — Verify message_thread_id fix works in next session
5. **100+ games** — Clarify if games were lost during import or original count included non-game items
6. **n8n payment (€25.68)** — 2nd notice received, payment pending
7. **GitHub Deploy failing** — PSVIBE-API-Server master branch deploy workflow failing
8. **Shell.ps-vibe.com** — Boss needs to add Public Hostname in Cloudflare Zero Trust → Tunnels

## Key Technical Notes
- **Dashboard only supports 4 statuses** — `final_status` must be: Available, Damaged, Lost, In Use
- **games_library vs console_games** — Two independent tables; names must match between both for queries to work
- **Cloudflare API Token** — Token valid but server's WARP IP (104.28.163.33), not matching token IP restriction
- **Timezone reminder:** ALL timestamps UTC unless noted. Boss is UTC+6:30 (Myanmar)
- **Can't modify disc_count** — Boss explicitly prohibited; FC 26 disc_count=10 is intentional
- **IP unreachable not just port blocked** — ping also fails, so it's routing/filtering, not DPI

## Files Modified (June 12 session)
- `/root/psvibe-sales-bot/customer_bot/handlers.py` — FAQ intercept fix, text var fix, FAQ disable
- `/root/psvibe-sales-bot/customer_bot/api.py` — Game library data mapping
- `/root/psvibe-sales-bot/customer_bot/games.py` — AI prompt names fix
- `/root/psvibe-sales-bot/bot/handlers/sales.py` — Booking wallet skip (line 1677/1684)
- `/root/psvibe-sales-bot/bot/handlers/booking_flow.py` — _remind_loop restored, _cancel_remind added, message_thread_id
- `/root/psvibe-sales-bot/bot/handlers/session_reminder_store.py` — message_thread_id in persistence
- `/root/psvibe-sales-bot/bot/handlers/booking.py` — message_thread_id in timer creation
- `/root/psvibe_api_server/app.py` — /shell/ proxy routes (lines 3021+)
- `/etc/ssh/sshd_config` — Port 22 uncommented, port 80 added for SSH testing
- `/etc/cloudflared/config.yml` — shell.ps-vibe.com ingress, protocol http2
- `/var/spool/cron/crontabs/root` — Low stock alert reduced to 4h
- MySQL: `games_library` (41 rows), `console_games` (68 rows) — titles corrected
- `/home/node/.openclaw/workspace/smart_reminder.js` — Thread ID fixed to 125192

## What Kora Learned
1. **Dashboard JS only supports 4 statuses** — future frontend changes must stay within Available/Damaged/Lost/In Use
2. **SSH over non-standard port still detected by ISP DPI** — SSH protocol handshake is identifiable regardless of port
3. **SQL game titles must match exactly across tables** — games_library and console_games need identical names
4. **Booking customers should skip wallet check** — they pay at booking time, not at session-end
5. **disc_count values are intentional** — don't "fix" them without explicit Boss confirmation
6. **Cron interval changes need patience** — check after 1 cycle to confirm before marking done
7. **Staff group Forum mode** — if group has topics, messages without message_thread_id go to General topic where staff may miss them
8. **Entire IP can be unreachable (not just port)** — when ping also fails, it's routing/ISP-level blocking, not DPI
9. **Web SSH works even when direct SSH doesn't** — cloudflare tunnel + HTTPS bypasses ISP routing filters
