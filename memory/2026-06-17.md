# 2026-06-17 Daily Log

## 🛑 Critical Incident: Kora Giving Wrong Answers (Flash Model Issue)

### Problem
Boss pointed out Kora has been giving inaccurate/wrong answers recently. Root causes identified:
1. **Main session model was Flash** (`deepseek/deepseek-v4-flash`) — not suitable for complex analysis
2. **Kora doing analysis directly** instead of delegating to Pro sub-agents
3. **Overconfidence without verification** — jumping to conclusions

### Changes Made
1. **Config:** Changed `agents.defaults.model.primary` from `deepseek/deepseek-v4-flash` → `deepseek/deepseek-v4-pro`
2. **GOLDEN_RULES.md:** Added Rule #1a — 🚫 NEVER Analyze Complex Data with Flash Model
3. **AGENTS.md:** Added Pre-Response Verification checklist (4-step mental check before answering complex analysis)
4. **SOUL.md:** Added hard rule about complex analysis → Pro sub-agent delegation
5. **AGENTS.md:** Updated fallback chain to reflect Pro as primary

### Key Lesson
Flash model is fine for simple chat/lookups but **NOT for complex reasoning, financial numbers, code analysis, or multi-step deduction.** Must always delegate to Pro sub-agents for these tasks.

### Current Model Chain
- **Main:** DeepSeek V4 Pro (primary) → Flash → Gemini 2.5 Flash → Gemini 3.5 Flash
- **Sub-agents:** DeepSeek V4 Pro (primary) → Flash → Gemini 2.5 Flash → Gemini 3.5 Flash

## Booking Flow Fixes (14:35 UTC)

### Confirmed by Boss:
- Staff booking auto-confirm (source="staff") → CORRECT, keep as-is
- Customer booking → pending approval → CORRECT design
- All other issues → fix

### Bugs Fixed:
1. **`booking.py` L385: Typo "ယနော" → "ယနေ့"** — step_sbk_dur back button showed broken Burmese
2. **`booking.py` L411: Encoding fix** — `\nေန့ရက်` → `\nနေ့ရက်အသစ် ရွေးပါ` (Burmese vowel order encoding)

### Verified as Correct:
- Staff auto-confirm: sale bot sends `source: "staff"` → API sets `_bk_status = "confirmed"` ✅
- Customer pending: no source → API defaults to `"pending"` → staff must approve ✅
- Approval workflow: admin_bookings.py `_do_booking_action` handles approve/reject with console auto-assign + customer notification ✅
- Slot conflict check: api_bookings_create checks MySQL for overlapping bookings ✅
- Date format handling: API handles M/D/YYYY, YYYY-MM-DD, and other formats ✅
- n8n reminder webhook: fires on both staff-create and admin-approve ✅
- Customer "My Bookings": shows pending/confirmed/expired with cancel option ✅
