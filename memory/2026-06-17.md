# 2026-06-17 Daily Log

## ☀️ နံနက်ခင်း System Report (01:37 UTC / 08:07 MMT)

### 📊 PS VIBE Sales
- ရောင်းရငွေ: **708,100 MMK** (28 transactions)
- Top-up: ၂ ခု၊ **180,000 MMK** (1,200 mins)
- Active members: **၇ ယောက်**
- ဒီနေ့ booking: **၁ ခု**

### 🖥️ Infrastructure
- Docker: **7 containers, အားလုံး healthy** ✅
- Disk: **25%** (36G/150G used)
- Memory: **4.6/15 GB** — ပုံမှန်
- Console: ၁၀ လုံး free
- All services healthy (check_alerts.py: ✅)
- Dead-letter queue: empty ✅
- Pending tasks: 0 ✅
- Tasks in progress: 0 | Stuck: 0 | Orphans: 2 (known)

### 🔧 Memory System
- Index rebuilt: 1,172 topics
- Knowledge graph: 54 nodes, 1,420 edges
- Pruner: nothing to prune
- Git backup: committed 6 files

### ⚠️ သတိထားရန်
- Quality Gate: **58/100** (up from 55 — improving)
- API Server Git: **3 uncommitted changes** ⚠️
- YYO Wallet alerts (7 unread — known ongoing issue, no instance running)
- HEARTBEAT.md paths still use `/home/node/` should be `/root/`

## P&L Fix — Game Profit includes Wallet Consumption
- Boss က P&L မှာ Game Profit က Game Revenue ကော Wallet Consumption ကော ထည့်တွက်သင့်တယ်လို့ မေးလာတယ်
- Fix: `dashboard_routes.py` line 2287 — `game_profit = game_rev` → `game_profit = game_rev + wallet_consumed`
- **Logic:** Wallet top-ups = Deferred Revenue; Wallet consumption = Recognized Revenue (member သုံးတဲ့ minutes)
- API restarted ✅

## Financial Validation Cron Setup
- Script: `/root/psvibe_api_server/scripts/financial_validation.py`
- Checks: A=L+E, CA=FB=CF, Retained≈P&L
- Cron: Every day 03:00 Asia/Yangon (openclaw cron job `daily-financial-validation`)
- Silent when all good. Only messages Boss if discrepancies found.
- Delivery: none (no auto-announce). FailureAlert after 2 consecutive failures.

## P&L + Balance Sheet + Cash Flow Consistency Fix

### Problems Found & Fixed:
1. **Retained Earnings formula**: Removed `member_liab`, removed auto-balance, added `wallet_consumed`, excluded topups from revenue
2. **Rent Amortization double-count**: KBZ was deducted BOTH via prepaid deduction AND opex (2.49M) — fixed by excluding it from opex query
3. **WavePay account name mismatch**: BS used "WavePay" but DB stores "Wave" — transfer_out 95K missed — fixed
4. **ACM's Acc opex normalization**: "acm's_acc" → "acm_acc" in opex parsing — 415K missed in both BS and CF — fixed

### Final State:
- **BS CA = FB = CF**: All three perfectly match at -10,801,469 ✅
- **A ≈ L+E**: 33,216 gap (0.01%) — minor rounding
- **Retained Earnings**: -10,563,923 vs P&L Net Profit -10,380,590 — diff 183,333 (April depreciation not in June P&L)
- **P&L Game Profit**: Now includes wallet consumption ✅
- Boss reported: Confirmed bookings in Sales Bot don't show customer names. Admin Group notification shows names (like "Yoon", "Naing Htet Aung") but Sales Bot's `/confirm` shows only 📞 phone numbers.

### Root Cause Found — `COALESCE('', name)` Trap
- **Location:** `app.py` line 1335 — `PATCH /api/bookings/{id}/status` endpoint
- "staff_name=COALESCE(%s, staff_name), notes=COALESCE(%s, notes)"
- **Flow:**
- 1. Customer bot creates booking → POST with `customerName: "Naing Htet Aung"` → INSERT stores `staff_name = "Naing Htet Aung"` ✅
- 2. Admin notification sent with correct name ✅
- 3. Staff clicks "Approve" → PATCH with `{"status":"confirmed","staffNote":"Approved by Osmo"}` — NO `staffName` field
- 4. `req.get("staffName", "")` → `""` (empty string)
- 5. `COALESCE('', 'Naing Htet Aung')` → `''` 💀 **NAME WIPED!**
- 6. Same for `notes`: `COALESCE('', 'Game: FC 26 | Phone: ... | User: ...')` → `'Approved by Osmo'` 💀

### The Fix
- "staff_name=COALESCE(NULLIF(%s, ''), staff_name), notes=COALESCE(NULLIF(%s, ''), notes)"

### Names Recovered from Server Log
- Server log at `/root/psvibe_api_server/server.log` had `logging.warning("api_bookings_create: customer_name=%s", ...)` entries. Cross-referenced log entries with DB bookings by date/time/console:
| Booking ID | Name Recovered | Log Match Confidence |
|-----------|---------------|-------------------|
| #447 | Naing Htet Aung | ✅ Exact (same date/time, console changed from C-04→C-01 auto-assign) |
| #444 | Jake | ✅ Exact (17:30, console C-08→C-01 auto-assign) |
| #442 | Lolly | ✅ Exact (June 19, 12:00, C-10) |
| #435 | Aung Myo Thant | ✅ High (June 19, 12:00, C-02→C-03 auto-assign) |
| #431 | Nay Lin Khant | ✅ High (June 18, 12:00, C-03→C-05 auto-assign) |
| #429 | Phyo Wai | ✅ Exact (June 18, 11:00, C-01) |
| #428 | Zane | ✅ High (June 17, 10:30, C-01; time 10:42, close) |
| #434 | Nyan Lin Htet | ✅ High (June 17, 12:00, no console; time 11:52, close) |
- Total 13 customer bot bookings today. 8 had names recovered from log. Remaining: #430, #436 (cancelled), #439 (console mismatch), #414 (old, no log).

### Customer Bot Fix
- Also fixed customer bot to force name entry when member record has empty name:
- `_lookup_and_confirm_member()`: member name empty → redirect to BK_NAME instead of showing confirm
- Phone verify single match: same logic applied
- **File:** `/root/psvibe-sales-bot/customer_bot/booking_handlers.py`

### Services Restarted
- `psvibe-api` ✅ (restarted for PATCH fix, list endpoint phone fallback)
- `psvibe_customer_bot` ✅ (restarted for name force-entry fix)

### Critical Lesson
- **`COALESCE('', val)` returns `''` because empty string IS NOT NULL in SQL.** When using COALESCE as "use this new value, or keep existing if not provided", always wrap the new value with `NULLIF(%, '')` to treat empty string as "not provided."


---

## 🔧 Memory System Audit & Repair (09:00+ UTC)

Boss asked: "Why does Kora forget everything on new session?"
→ Full memory system audit conducted. Major issues found and fixed.

### P0 Fixes:
1. **MEMORY.md Bloat (115 KB → 4 KB)** — Full daily logs (Jun 3-17) duplicated in MEMORY.md instead of keeping card catalog only. Caused session startup truncation → losing recent context. **Fixed:** Archived duplicated content, kept card catalog only.

2. **ALL CRON Paths Broken (/home/node/ → /root/)** — 22 file occurrences + 14 cron jobs used non-existent `/home/node/.openclaw/workspace/` paths. **Fixed:** All to `/root/`.

3. **Gmail Morning Check — 3 consecutive errors** — Missing `to` field in delivery config. **Fixed.**

4. **daily-git-backup timeout** — No timeout limit set. **Fixed:** Added timeoutSeconds:60 + lightContext.

### P2 Fixes:
5. **2026-06-17.md duplicate content** — File had duplicated sections. **Fixed:** Deduplicated.
6. **heartbeat-procedures.md stale paths** — 2 `/home/node/` references. **Fixed.**
7. **AGENTS.md** — Added Session Startup auto-run steps (boot protocol + memory search).
8. **memory-index.json** — Rebuilt clean.
9. **MEMORY.md backup preserved** — at MEMORY.md.backup (original 115KB).

### Restored from Backup:
- Booking fix section restored to this daily log (was only in MEMORY.md, not in daily log).
- Critical lesson about `COALESCE('', val)` → `COALESCE(NULLIF(%s, ''), val)` added to lessons.md.
