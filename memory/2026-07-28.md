# Memory — 2026-07-28 (Tuesday)

## 🔧 Deposit Overview: Net Position double-count fix
- **Deposit Overview** page's ⚖️ Net Position card had formula: `active_deposits_total − this_month_forfeited`
- Bug: forfeited deposits are already excluded from `active_deposits_total` (filtered by `deposit_status IN ('paid','verified')`), so subtracting again = double-count
- **Fix (Option D):** Changed to just show `active_deposits_total`, removed `- this_month_forfeited`
- All-time stats section (at bottom) already correctly shows forfeited/refunded separately

## 🐛 Rebook Deposit Restore — _mysql_exec Typo Bug
- **Root cause:** `dashboard_routes.py:353` used `_mysql_exec` but import is `_mysql_execute` — NameError silently crashed
- **Effect:** When staff rebooked auto-cancelled bookings (cancelled→confirmed), deposit restore ran `_mysql_exec()` which doesn't exist → error thrown but booking status already updated → deposit stayed "forfeited"
- **Affected bookings:** BK#2083 (3,000 Ks), BK#2084 (3,000 Ks), BK#1898 (7,000 Ks) — total 13,000 Ks
- **Fix:** Changed `_mysql_exec` → `_mysql_execute`, restored deposits via direct DB query, restarted psvibe-api

## 📧 Gmail OAuth Re-authorization
- Refresh token expired/revoked ("invalid_grant")
- Re-authorized Gmail API via OAuth flow — new token saved to token.json
- Token exchange successful, emails readable again
- Refresh token should now be valid long-term

## 📧 Coco Email — Construction Bot Timber Added
- Coco (rein020124@gmail.com) added 'Timber' to MATERIAL_TYPES in construction bot
- Bot restarted on Three Brothers server
- Reply sent confirming restart success
