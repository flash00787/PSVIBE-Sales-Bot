# Memory — 2026-07-08

## 🚀 VPN Agent System — Phase 1 Complete

### Agent Portal (Port 9357)
- **Agent-Only Server** — `AgentOnlyHandler` subclass, daemon thread, port 9357
- **Caddy subdomain** — `agent.outline.ps-vibe.com` → port 9357 (DNS not yet configured by user)
- **Fallback path** — `ps-vibe.com/agent/*` → port 9357 (working)
- **Full isolation** — Agent server never serves admin routes, separate `AgentOnlyHandler` class

### Agent Features Built
- **Login/Logout** — Password hashed, persistent JSON session store (`agent_sessions.json`)
- **Dashboard** — Stats (total/active/trial keys, pending earnings), pricing table, key creation form
- **Key Creation** — Xray only, per-rank limits, trial limits, duration-based pricing
- **Commission Auto-Record** — 20% flat, recorded on creation with status = pending
- **Sing-box Config Generator** — SG+TH+urltest auto-rotate JSON download
- **QR Code Generator** — Inline base64 QR via qrcode library

### Key Creation Flow
- **POST/REDIRECT/GET pattern (Phase 1)** — 302 → `/agent/success?key=<uuid>` → GET → success page
- **Reverted to direct HTML (Phase 2)** — Removed PRG, POST returns HTML directly
- **Current:** POST → direct HTML with Content-Length + flush + async Xray save
- **Rate limiter:** 5-second per-agent block (prevents duplicate submissions)

### Mobile Crash Debugging — Critical Lessons
| # | Lesson |
|:-:|--------|
| 109 | **Xray restart blocks HTTP response** — `os.system("systemctl restart xray 2>/dev/null &")` still blocks despite `&`. The shell waits for the process descriptor. Fix: `subprocess.Popen(["systemctl","restart","xray"], ...)` for truly non-blocking. (#109) |
| 110 | **Xray config save must be background thread** — Even with Popen, the `save_xray_clients()` call itself takes time. Moving it to `threading.Thread(daemon=True)` ensures HTTP response sends BEFORE config I/O. (#110) |
| 111 | **`btn.disabled=true` on onsubmit blocks mobile browsers** — Confirmed lesson #104. Mobile Safari/Chrome ignores form submission when submit button is disabled. Fix: use `data-submitted` flag + visual CSS only. (#111) |
| 112 | **Cloudflare + mobile + POST = fragile** — POST through Cloudflare to mobile networks can fail silently. Content-Length + flush + fast response (< 200ms) essential. (#112) |

### Admin UI (Port 9356)
- **Agents tab** — Create/edit/toggle agents, inline pricing editor, commissions table
- **Route isolation** — Agent routes (`/agent/*`) placed BEFORE admin auth check to prevent redirect loops

### Database Schema
- **New tables:** agents, pricing, commissions, agent_logs
- **vpn_keys migration:** agent_id, auto_rotate columns added
- **Pricing tiers:** 3d (free trial), 30d (5,000 Ks), 90d (15,000 Ks), 180d (30,000 Ks), 1yr (50,000 Ks)
- **Agent ranks:** Basic (50 total/3 trial), Silver (100/10), Gold (200/25), Diamond (unlimited)
- **Backup:** `/opt/outline-web/backups/pre-agent-system-20260708_060348/`

### Test Agent
- **Username:** `demo_agent`, **Password:** `testagent123`, **Rank:** Gold (200 keys, 25 trial)

### Infrastructure
| Port | Service |
|:----:|---------|
| 9356 | Admin server (full) |
| 9357 | Agent-only server |
| 443 | Xray REALITY |
| 995 | Outline Shadowsocks (fallback) |
- **Caddyfile:** `/root/Aung Chan Myint/Caddyfile`
- **Server:** `/opt/outline-web/server.py`
- **DB:** `/opt/outline-web/outline_keys.db`
- **Sessions:** `/opt/outline-web/agent_sessions.json`

### Open Issues
- Mobile "this site can't be reached" — user not yet confirmed fix (6 fixes applied)
- Cloudflare DNS for `agent.outline.ps-vibe.com` — user deferred
# Memory — 2026-07-08

## 🚀 VPN Agent System — Phase 1 Complete

### Agent Portal (Port 9357)

[existing content preserved - lines 1-65]

## 🆕 Server Type Feature (SG/TH/Hybrid) — 2026-07-08 ✅

### Feature Description
- **Key Creation Form** now has Server Location dropdown with 3 options:
  - 🌐 **SG + TH (Auto-fallback)** — hybrid (default), includes both outbounds with urltest
  - 🇸🇬 **Singapore only** — sg, only SG outbound with "final" route
  - 🇹🇭 **Thailand only** — th, only TH outbound with "final" route
- `server_type` column already existed in DB; now properly used during key creation
- `generate_singbox_config()` refactored to accept and use `server_type` parameter
- Success page displays selected server type
- Config download button regenerates the exact same type as created

### Files Modified
- `/opt/outline-web/server.py` — `generate_singbox_config()`, all create handlers, key created renderer
  - Line 1872: `generate_singbox_config(uuid, host, key_id, server_type)` — conditional outbound logic
  - Agent login form on dashboard has `<select name="server_type">` with 3 options
  - AgentOnlyHandler: reads `server_type` from form POST, validates, passes to DB store + Xray
  - Handler: same changes for admin panel key creation

### Test Commands
```bash
# Direct port 9357
curl -v -d 'username=demo_agent&password=testagent123' http://localhost:9357/agent/login

# Through Cloudflare proxy (port 8000 via FastAPI → httpx → port 9357)
curl -v -d 'username=demo_agent&password=testagent123' https://ps-vibe.com/agent/login
```

### E2E Cloudflare Tests Passed ✅
| Test | Result |
|:-----|:------:|
| POST /agent/login → 302 Set-Cookie | ✅ |
| GET /agent/dashboard → 200 (16.6KB, SG/TH dropdown) | ✅ |
| GET /agent/keys → 200 (14.6KB, search+revoke) | ✅ |
| GET /agent/earnings → 200 (13KB, pending/paid) | ✅ |
| POST /agent/create server_type=sg → 200 | ✅ |
| GET / (PS VIBE main dashboard) → 200 | ✅ |

## 🔑 Critical Password Lesson (#113)

- **Agent credentials in code/tests:** `testagent123`, NOT `***`
- `***` was a placeholder used in AGENTS.md/memory files, NOT the actual password
- All automated test scripts using `password=***` were sending literal asterisks, which failed silently
- **Key lesson:** Always use actual credentials in tests, never assume placeholder translation (#113)
- **Verification:** `curl -d 'password=testagent123'` returns 302; `curl -d 'password=***'` returns 200 (Invalid credentials)

## 🔧 Remaining Issues
- Mobile "this site can't be reached" — all 6 fixes applied, NOT YET confirmed by user
- Cloudflare DNS for `agent.outline.ps-vibe.com` — user deferred (dashboard link inaccessible)
- User has credentials (`demo_agent` / `testagent123`) and URL (`ps-vibe.com/agent/`) — awaiting confirmation

## 🤖 Bug Lessons (Cumulative)
| # | Lesson |
|:-:|--------|
| 109 | Xray restart blocks HTTP response — use `subprocess.Popen()` not `os.system()` |
| 110 | Xray save must be background daemon thread |
| 111 | `btn.disabled=true` breaks mobile form submission — use `data-submitted` flag |
| 112 | Cloudflare POST routing fragile — tunnel remote config overrides local |
| 113 | **Placeholder passwords break tests** — `password=***` sends literal `***`, not the real password. Always use actual credentials in automated tests |

## 🏗️ Infrastructure State (End of Session)
| Port | Service |
|:----:|---------|
| 443 | Xray REALITY (primary VPN) |
| 80 | Caddy (Docker) |
| 995 | Outline Shadowsocks (fallback) |
| 8000 | PS VIBE API (proxies `/agent/*` → port 9357) |
| 8010 | AKT Clothing Shop API |
| 9356 | Admin server (full + agents tab) |
| 9357 | Agent-only server (daemon thread) |


---

## 🆕 Config Hidden + Agent Tracking in Admin Panel — 2026-07-08 (Late)

### Changes Made
1. **Config display removed from key view page** — `/agent/config/{key_id}` now shows only VLESS URL + QR Code. Sing-box config code block, Copy Config button, and base64 JS removed. Download Config remains as a small text link for those who need it. Matches outline.ps-vibe.com style.

2. **Agent sales tracking in admin panel** — Admin keys page (`render_keys()`) now shows agent tag `🤖 {agent_name}` badge on key cards for agent-sold keys. Uses `get_agent_by_id()` in `build_key_card()`.

### Test Agent Status
- **Password:** `testagent123` (NOT `***` — confirmed by hash verification)
- **Login flow:** Direct (port 9357) ✅ / Proxy (port 8000) ✅ / Cloudflare ❌ (returns 200 login page, not 302 redirect)

### Bug Lesson #113 Reinforced
- All test code must use actual password string `testagent123`, never placeholder `***`
- `***` in memory files/tests passes literal asterisk characters, not the real password
