
## Memory (2026-07-17) — SEL Equity Modal Fixes + PS VIBE Booking Fixes 🔧

### SEL Exchange — Equity Modal Auto-Show Bug 🐛→✅
- **Problem:** 4 equity modals (inject, eject, dividend, shareholder) had no `style="display:none"` inline — CSS class `.modal-overlay{display:flex}` made them all visible on page load, blocking all tab clicks.
- **Fix 1:** Added `style="display:none"` to all 4 equity modal divs.
- **Fix 2:** Created `openEquityModal(id)` function — shows equity-specific modal by ID.
- **Fix 3:** Fixed `closeModal(e)` to handle string ID arguments (equity modals pass strings, main modal passes Event).
- **Fix 4:** Updated all 4 equity open handlers to use `openEquityModal()`.
- **Lessons:** All new modals must have `style="display:none"` inline. Modal open/close functions must be compatible with the modal architecture (shared overlay vs individual overlays).

### PS VIBE — Customer Bot Booking Visibility & Web Dashboard Confirm 🐛→✅
- **Fix 1:** `search-bookings` filter (line 1107) — changed `deposit_status = 'paid'` → `deposit_status IN ('paid', 'verified')` to show verified bookings on timeline.
- **Fix 2:** Merged `api_bookings_create` format detection — web dashboard detected first via `is_web_format` check (`booking_date`, `customer_name`), before customer bot format check.
- **Fix 3:** Removed duplicate web dashboard format block in `else` branch (was `else:` fallthrough to old web format after merge).
- **Lessons (#159):** Web dashboard JS may send camelCase `customerName` — merged handler must check web-specific fields first to avoid entering bot branch.
- **Lessons (#160):** Deposit verification changes status from 'paid' to 'verified' — all filters checking for 'paid' must also include 'verified'.

### Infrastructure
- SEL Exchange API (`sel-exchange-api`) restarted after dashboard fix.
- PS VIBE customer bot (`psvibe-customer-bot`) restarted.
