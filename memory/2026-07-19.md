# Memory 2026-07-19 (Sunday) — Ko Toe Ban & English Group TTS Fix

## 🎙️ English With Kora — TTS Voice Auto-Generate Fix
- **Problem:** Group session was responding with text corrections only, no TTS voice audio
- **Root Cause:** AGENTS.md didn't have TTS instructions for the English group session — group auto-responder didn't know to call `tts` tool
- **Fix:** Added `## 🎙️ English With Kora Group — TTS Voice Rules` section to AGENTS.md
- **Instructions:** When correcting English in group -1004466631733, ALWAYS call `tts(text: corrected_sentence)` — voice auto-attaches
- **Status:** ✅ Fixed by workspace bootstrap injection

## 🚫 Ko Toe (8806200022) — Permanent Ban

### Background Investigation
- Booking #1862 (C-09, 19:00-03:00) and #1864 (C-10, 20:00-04:00) were both PENDING
- Both booked by same Telegram account: `8806200022` (Ko Toe)
- Ko Toe is NOT a registered staff member — our staff_records has only 3 staff (Ko Khant, Eaindray, Min Myat Naing)
- Ko Toe used the Customer Bot (not staff sale bot) to create bookings
- staff_name "KoMyo" was just the name he entered during customer bot booking flow

### Booking History (All 6 — ALL CANCELLED)
| ID | Date | Console | Time | Status | Deposit |
|:--:|:----:|:-------:|:----:|:------:|:-------:|
| #1475 | Jul 12 | C-02 | 14:00-15:00 | Cancelled | — |
| #1479 | Jul 8 | C-08 | 12:00-20:00 | Cancelled | — |
| #1590 | Jul 18 | C-10 | 20:00-04:00 | Cancelled | — |
| #1679 | Jul 15 | C-10 | 19:00-03:00 | Cancelled | 29K refunded |
| #1862 | Jul 19 | C-09 | 19:00-03:00 | Cancelled | — |
| #1864 | Jul 19 | C-10 | 20:00-04:00 | Cancelled | — |

### Ban Applied
1. `handlers.py` — Added `8806200022` to `BLOCKED_IDS` set
2. `main.py` — Added `filters.User(user_id=8806200022)` to `_blocked_user_filter` (group=-1)
3. DB: `customer_bot_users.is_active=0` for tg_id=8806200022
4. Bookings #1862, #1864 → cancelled
5. `psvibe-customer-bot.service` restarted

### Block Verification (3 Layers)
**Layer 1 — PTB Framework Filter (group=-1, highest priority):**
- `MessageHandler(filters.User(user_id=8806200022), _block_user)` at group=-1
- Catches ALL message types from 8806200022 (text, command, photo, reply keyboard)
- Runs BEFORE ConversationHandler (group=0) and any other handler

**Layer 2 — Code-Level Guards:**
- `cmd_start()` → `_is_blocked()` check
- `cmd_menu()` → `_is_blocked()` check  
- `handle_menu_buttons()` → `_is_blocked()` check

**Layer 3 — DB State:**
- `is_active=0` confirmed
- Both bookings marked cancelled

### Potential Bypasses Checked
| Path | Risk | Verdict |
|:-----|:----:|:-------:|
| CallbackQuery (inline buttons) | ⚠️ MessageHandler misses it | ✅ No risk — bot uses ReplyKeyboardMarkup (text) |
| Edited Messages | ⚠️ | ✅ MessageHandler catches by default |
| Active Conversation States | ⚠️ | ✅ Bot restart cleared all states |

**Lesson #194:** When banning a customer bot user, MUST update BOTH `BLOCKED_IDS` (handlers.py) AND `_blocked_user_filter` (main.py) — the handlers.py check is code-level, main.py filter is PTB framework-level. Also handles inline button flows separately from message flows.

## 🔧 System Config
- English Group (Telegram: -1004466631733) → `systemPrompt` couldn't be set via gateway (protected paths)
- Workaround: AGENTS.md bootstrap file with per-group TTS instructions
- Customer bot block mechanism: dual-layer (handlers.py + main.py)
- Allowed staff TG IDs: 5756649112, 6296803251, 7421933787, 8336350778, 8528272547, 8539344655, 8585297078
