# Daily Memory — 2026-07-16

## Morning Session (04:30-09:50 UTC / 11:00-16:20 MMT)

### Discord Bot Token Fix (Osmo)
- **Service:** `psvibe-discord-bot`
- **Issue:** Restart loop — "An invalid token was provided"
- **Fix:** Updated `DISCORD_BOT_TOKEN` in `/etc/psvibe/secrets.env` with new token from Discord Developer Portal
- **Status:** ✅ Bot ready — Birthday cron + LFG + leaderboard all active

### Reminder Spam Fix (Osmo) — 3 Bugs Fixed 🔧

**Bug 1: `fire_count` reset mid-loop**
- `_sync_duration_from_db()` was called AFTER `fire_count+=1` but BEFORE `send_message`
- Duration change reset `fire_count=0` → message sent with `fire=0` → overdue calc = `(0-2)*5 = -10`
- **Fix in `session_reminder.py`:** Moved sync call BEFORE `fire_count+=1`. Added proper fire_count recalculation from remaining seconds.

**Bug 2: `_remind_key` vs `remind_key` key mismatch**
- `_remind_key()` returned `f"{cid}|{chat_id}"` (no abs)
- `remind_key()` returned `f"{cid}|{abs(chat_id)}"` (with abs)
- Caused `sync_api_reminders_async()` dedup to fail for store entries with `chat_id=0`
- **Fix in `session_reminder.py`:** Added `abs(chat_id)` to `_remind_key()`

**Bug 3: `extend-duration` API didn't sync bot reminder store**
- Web dashboard duration changes only updated MySQL DB
- Old `_remind_loop` kept running with stale `end_t` from `session_reminders.json`
- **Fix in `booking_routes.py`:** `extend-duration` endpoint now updates bot's `session_reminders.json` with new end_t

**Files modified:**
- `/root/psvibe-sales-bot/bot/handlers/session_reminder.py` — Fixes 1 & 2
- `/root/psvibe_api_server/routes/booking_routes.py` — Fix 3

**Services restarted:** `psvibe-sale-bot`, `psvibe-api`
**Lessons:** #169, #170, #171 added to MEMORY.md
**Docs:** auto-doc updated (Fix #159)

### Current Sessions (09:52 UTC / 16:22 MMT)
| Console | Member | Plan | End | Status |
|---------|--------|------|-----|--------|
| C-07 | Thura zaw2 | 360 min | 17:52 MMT | Restored ✅ |
| C-08 | Thura Zaw3 | 360 min | 17:53 MMT | Restored ✅ |
| C-03 | Sai Hut Aung | 360 min | 19:07 MMT | Restored ✅ |
| C-04 | Aung Bhone Myat | 60 min | 16:34 MMT | Restored ✅ |
| C-05 | Aa | — | 17:01 MMT | Restored ✅ |
| C-06 | Guest | — | 17:10 MMT | Restored ✅ |
| C-10 | Hay Thi | — | 19:13 MMT | Restored ✅ |
