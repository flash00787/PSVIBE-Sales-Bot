# 2026-06-21 Daily Log

## Session Start Booking Link — 4 Bugs Fixed (11:00-11:30 MMT)

### Context
Boss reported: "Session start" နှိပ်ရင် checked-in customers နဲ့ link ဖို့ optional prompt တစ်ခုလိုတယ်။ Timeline မှာ checked-in နဲ့ session start သပ်သပ်ဖြစ်ပြီး conflict ဖြစ်နေတယ်။

Flow already existed (`prompt_book_link`) but was NEVER working due to 4 cumulative bugs.

### Fixes Applied

| # | Bug | File | Root Cause | Fix |
|---|-----|------|-----------|-----|
| ① | checked_in bookings not fetched | `booking.py:1020-1021` | Only `status=confirmed` queried | Added `status=checked_in` API call |
| ② | Date format mismatch (CRITICAL) | `booking.py:1018` | `today_str()` → `"6/21/2026"` vs API `"2026-06-21"` | `now_mmt().strftime("%Y-%m-%d")` |
| ③ | dict+list TypeError | `booking.py:1025` | Single checked_in returns dict, can't concat with list | Safe type checking + unwrap |
| ④ | customerName not used | `booking.py:1078,1158` | `b.get("memberId")` → always None (field doesn't exist in API) | `customerName` > `member_id` > `memberId` > "Guest" |

### Additional Changes
- Skip button moved to TOP: `"⏭️ Booking လုပ်ရန်မလိုဘဲ ဆက်သွား"` first row
- checked_in bookings always linkable (no 30-min window restriction)
- Matched handler text for new skip button label
- `consoleId` fallback added for mixed case API fields

### Files Modified
- `/root/psvibe-sales-bot/bot/handlers/booking.py` — `prompt_book_link()`, `step_book_link()`

### Key Lesson
- **API field naming is inconsistent:** camelCase (`customerName`, `timeSlot`) mixed with snake_case (`member_id`, `console_id`) in same response
- `today_str()` format `M/D/YYYY` is different from API's `YYYY-MM-DD` — don't mix them in comparisons
- ALWAYS verify API field names exist before using `.get()` — `memberId` never existed but was used everywhere

### Previous Fixes (before 11:00 MMT)
- Reject reason text input working (group=-2 fix + arg order fix)
- Timeline auto-confirm on staff bookings
- Game dropdown on timeline page
