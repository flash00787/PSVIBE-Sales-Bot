
## 🔧 Fixes Deployed (Morning Session ~06:00-09:00 UTC)

### 1. Timeline Vue SPA — 429 Rate Limit ✅
- **Root cause:** 16 parallel API calls to `/api/bookings` → Cloudflare 429
- **Fix:** 7 separate status calls → 1 single call + JS `.filter()` grouping
- **Files:** `timeline.ts`, `TimelineView.vue`

### 2. BranchStore 401 Unauthorized ✅
- **Fix:** `axios.get()` → `apiClient.get()` (adds JWT Bearer)
- **File:** `branchStore.ts`

### 3. Deposit Refund System — Built 🆕
- **Endpoint:** `POST /api/bookings/{id}/refund`
- **Rules:** 1hr before booking time only, checks deposit_status, auto cash_movements eject
- **New Status:** `deposit_status = 'refunded'`
- **New Helper:** `_notify_deposit_refunded()`
- **File:** `booking_routes.py`

### 4. Booking #1969 — Yaboku (@yatoeiji) Deposit Refund ✅
- cash_movements #484 inject +9,000 (Ko Khant verify)
- cash_movements #487 eject -9,000 (Refund)
- Net: 0 Ks

### 5. Deposit SS Forwarding — Bot Token Bug 🐛→✅
- **Issue:** deposit_ref (file_id) from CUSTOMER_BOT_TOKEN, but notification used BOT_TOKEN → cross-bot file_id rejected
- **Fix:** Use CUSTOMER_BOT_TOKEN for `sendPhoto`

### 6. Bookings & Deposit Group (New) 📱
- **Group:** `Bookings & Deposit (PS VIBE)` → `DEPOSIT_NOTIFY_CHAT=-1003611548363`
- **Routing:** Forfeited, Refunded, Verified notifications → new group
- **Env:** `/etc/psvibe/secrets.env`

### 7. Cancelled_at Backfill 🗄️
- **Bug:** 67 cancelled bookings had `cancelled_at = NULL` → front-end showed indefinitely
- **Fix:** Backfilled with `created_at`, front-end now hides if no cancelled_at
- **File:** `timeline.ts`

### 8. Refund Button UI 🖥️
- **Timeline Modal:** Shows for forfeited/paid/verified deposits, with 1hr time check
- **Bookings Management:** `↩️ Refund` button in actions column
- **Sorting:** Pending at top (Bookings page)
- **Search:** Added booking ID search
- **Files:** `TimelineView.vue`, `BookingsManagement.vue`, `dashboard_routes.py`

### 9. Cancel → Auto-forfeit Deposit 🔄
- **Fix:** Cancel endpoint now sets `deposit_status='forfeited'` for paid/verified
- **File:** `booking_routes.py`

### 10. Pending 15min Deposit Timeout ⏱️
- **New logic in auto-cancel-no-show cron:** Cancels pending bookings without deposit after 15 minutes
- **Customer notification via Telegram**
- **Slot freed for others**
- **File:** `booking_routes.py`

### 11. Refund Auto-tagging `[Refund]` 🏷️
- cash_movements.eject note format: `[Refund] BK#ID via KPay`
- Query: `WHERE note LIKE '%[Refund]%'`
- **File:** `booking_routes.py`

## 📚 Lessons Learned (Today)
| # | Lesson |
|:-:|--------|
| 195 | **429 Rate Limit from 16 parallel API calls** — Combine status-filtered calls into 1 + client-side .filter() |
| 196 | **Refund ≠ Forfeited** — Three states: `paid`, `forfeited`, `refunded`. Each has different accounting. |
| 197 | **Refund 1hr rule** — Booking မတိုင်ခင် 1hr အလိုအထိသာ |
| 198 | **Separate notification channels** — Deposit ops in dedicated group via DEPOSIT_NOTIFY_CHAT |
| 199 | **Cross-bot file_id** — Telegram file_ids are bot-specific. Forwarding requires same bot token that generated it. |
| 200 | **cancelled_at NULL → timeline showing forever** — Backfill with created_at, front-end must handle missing timestamps |
| 201 | **Pending deposit 15min timeout** — Pending bookings without deposit auto-cancel after 15min, freeing slot. |

## 🔜 What's Next
- Monitor auto-cancel-no-show cron (every 5 min)
- Watch for pending timeout in action
- Future: Bulk actions, auto deposit reminder, bot menu refund

## 🚀 Build Hash Timeline
| Bundle | Hash | Change |
|:---|:---:|:---|
| TimelineView | `AYRiQIR8.js` | Refund button |
| TimelineView | `BJkll-Jt.js` | Cancel auto-forfeit |
| TimelineView | `BKOgrziI.js` | CanRefund time check |
| TimelineView | `C2ke91AV.js` | pending deposit filter |
| TimelineView | `B1x6DgQZ.js` | cancelled_at fix |
| TimelineView | `DO4JVkph.js` | refunded hidden |
| BookingsManagement | `BhvnOn4k.js` | Refund btn + sort + search |
