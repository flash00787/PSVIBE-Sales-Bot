# Memory (2026-07-02)

## Session 4: Console Swap + Timer Reminder Fix + Business Overview (~12:46 – 14:20 PM MMT)

### Console Swap — Staff Permission + Detail Cards
- Added `/swap` to `staffOnlyPaths` in AppLayout.vue (staff couldn't see it)
- Added booking ID, phone, notes display on confirmed booking cards in SwapView.vue
- API: Added `phone` and `notes` fields to `GET /swap/confirmed` response
- Dashboard rebuilt + deployed

### Timer Duration Change — Reminder Conflict Hybrid Fix
**Root cause:** `PUT /bookings/{id}/timer` only updated DB, never touched reminder systems.
- **API fix:** `dashboard_adjust_timer()` now calls `session_timer.schedule_session_timer()` to cancel old API timer + schedule new one
- **Bot fix:** Added `_sync_duration_from_db()` in `session_reminder.py` — queries `fetch_console_status` before each reminder fire, auto-corrects `_SESSION_END_TIMES` and `_SESSION_TOTAL_MINS` if dashboard changed duration
- Also added custom minutes input to ConsoleTimer.vue ("✏️ Custom mins..." option with Enter key support)

### Business Overview — All-Time Cumulative Dashboard
- New endpoint: `GET /api/dashboard/cumulative` with 8 data sections
- New page: `/cumulative` → `CumulativeStats.vue`
- **Key PyMySQL lesson:** `%%` needed for literal `%` in `DATE_FORMAT` — PyMySQL treats `%Y` as format placeholder
- Revenue sources: `sales_daily.amount` (gaming), `food_cart.quantity*unit_price` (food), `topup_log.amount` (topup)
- Sections: Summary Cards, Monthly Trends, Top 15 Members, Top 10 Games, Revenue Mix, Console Utilization, Member Growth, Peak Hours
- All-time snapshot: ~13.3M Ks total revenue, 1053 bookings, 19 unique members

### Cancel Flow Fixes
- `_do_cancel_booking` (booking_flow.py): Added admin group notification via STAFF_NOTIFY_CHAT
- `_text_cancel_booking` (customer_bot/handlers.py): Added `_cancel_advance_reminder` call (was missing)

### Audit Findings (things already working)
- Customer Bot DOES send confirmation after booking (screenshot verified)
- No-show auto-cancel: cron runs `POST /api/bookings/auto-cancel-no-show` every 5 min (15-min grace)
- `_sbk_advance_reminder`: Disabled — n8n webhook handles reminders instead
- Food Menu `_psvibe_get_async` import: Already fixed with inline import
- Hardcoded admin chat ID: All uses `os.environ.get("STAFF_NOTIFY_CHAT", "-1003686032747")` — env var with fallback

### Files Modified
- `/root/psvibe-dashboard/src/components/AppLayout.vue` — sidebar nav items + staffOnlyPaths
- `/root/psvibe-dashboard/src/views/SwapView.vue` — confirmed bookings detail cards
- `/root/psvibe-dashboard/src/views/ConsoleTimer.vue` — custom mins input
- `/root/psvibe-dashboard/src/views/CumulativeStats.vue` — NEW all-time stats page
- `/root/psvibe-dashboard/src/router/index.ts` — cumulative route
- `/root/psvibe_api_server/dashboard_routes.py` — timer reschedule + cumulative endpoint
- `/root/psvibe-sales-bot/bot/handlers/session_reminder.py` — `_sync_duration_from_db`
- `/root/psvibe-sales-bot/bot/handlers/booking_flow.py` — admin cancel notify
- `/root/psvibe-sales-bot/customer_bot/handlers.py` — reminder cancel on self-cancel

### Lessons Learned
- **#63:** PyMySQL `cur.execute()` treats `%Y`, `%m` etc as format placeholders — escape as `%%Y`, `%%m` in `DATE_FORMAT` calls
- **#64:** Always check if a script/service already exists before planning new feature (auto-cancel was already running)
- **#65:** Dashboard timer change endpoint must interact with BOTH API timer (`schedule_session_timer`) AND bot state (`_sync_duration_from_db`)

---

- **Feedback submit API** က `created_at` မှာ MMT ပြောင်းဖို့ server-side `DATE_ADD(NOW(), INTERVAL 390 MINUTE)` သုံးထားပြီး


## Session 3: Feedback Bug Follow-ups (~12:30 PM – 1:00 PM MMT)

### 8. Feedback Star Selection Hangs — Missing edit_message_text ✅
**Bug:** Star ရွေးလိုက်ရင် bot ရပ်သွားတယ်။ `answerCallbackQuery` ပဲလုပ်ပြီး message update မဖြစ်ဘူး။
**Root Cause:** `cb_feedback_rating` function မှာ `thank_msg` build ပြီးတာနဲ့ function ရပ်သွားတယ် — `comment_kb` နဲ့ `edit_message_text` call ပျောက်နေတယ်။
Python script နဲ့ function replace လုပ်ရင်း အဆုံးပိုင်း truncate ဖြစ်သွားခဲ့။
**Fix:** Function အပြည့်အစုံ ပြန်ထည့် — `comment_kb` + `edit_message_text` + error handling။
**Lesson:** Function replace လုပ်တဲ့အခါ `_end` marker အတိအကျစစ်ဖို့၊ truncation မဖြစ်အောင် verify လုပ်ဖို့။
**File:** `/root/psvibe-sales-bot/customer_bot/handlers.py`

### 9. Feedback Time Double Conversion (7:14 PM → 12:44 PM) ✅
**Bug:** Feedback created_at က `7:14 PM` ပြနေတယ် — တကယ့်အချိန်က `12:44 PM` MMT ဖြစ်သင့်။
**Root Cause:** **Double Conversion** — 
- INSERT မှာ `DATE_ADD(NOW(), INTERVAL 390 MINUTE)` → DB ထဲ MMT သိမ်း (12:44)
- Dashboard SELECT မှာ `DATE_ADD(cf.created_at, INTERVAL 390 MINUTE)` → နောက်ထပ် +6:30 (19:14)
- API response မှာ `timedelta(hours=6, minutes=30)` → နောက်ထပ် +6:30
- DB ထဲ MMT သိမ်းထားပြီးသားကို SELECT & API က UTC→MMT ထပ်ပြောင်းနေ → Double = 13 hrs ahead!

**Fix:**
1. Old entries (#48-51) — UTC→MMT conversion in DB (`UPDATE customer_feedback SET created_at = DATE_ADD(created_at, INTERVAL 390 MINUTE) WHERE id < 52`)
2. `dashboard_routes.py` — `DATE_ADD(cf.created_at, INTERVAL 390 MINUTE)` ၅ နေရာ ဖြုတ် (WHERE, SELECT, GROUP BY)
3. API response `timedelta(hours=6, minutes=30)` ဖြုတ်
**Files:** `/root/psvibe_api_server/dashboard_routes.py`, DB migration

### Lesson Learned — ⚠️ Timezone Fix Pattern
- **Single source of truth:** Either store UTC + convert on read, OR store MMT + never convert. Never mix.
- When fixing timezone: **check ALL three layers** — INSERT, SELECT, API response
- **Old data migration:** Always migrate old rows BEFORE removing conversions from SELECT queries
- Lesson #60 extended: Not just INSERTs need MMT, but also verify no double-conversion in read paths

### Docs Update Status
- [x] Daily memory (2026-07-02.md) — 2 sessions, 9 fixes documented
- [x] auto_doc_updater.py — commit #179
- [x] MEMORY.md — timezone lesson appended to #60
- [ ] Project state docs — (not applicable, all inline fixes)
### ⏰ Cron: Stale Checkin Cleanup (21:00 MMT)
- API: POST /api/cleanup/stale-checkins (dry_run=false)
- Result: No stale check-ins found, 0 bookings cancelled
- Status: Clean
