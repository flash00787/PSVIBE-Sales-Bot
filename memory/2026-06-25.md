# 2026-06-25 — Daily Log

## Summary
Multi-project session: ACM Wallet MySQL migration completed, PS VIBE dashboard staff permission, customer bot booking bug fix.

---

## 1. ACM Wallet — MySQL Migration Complete ✅

### Phase 1-3 Done
- Database `acm_wallet` created with 7 tables
- Data migrated: 147 transactions, 44 opening balances, 56 settings
- `db.py` dual-write layer → full MySQL cutover
- `main.py`: 5,979 → 5,197 lines (-782)
- `budgets.get(uid)` bug fixed (4 instances → `db_get_budget(uid)`)
- 12/12 verification checks passed
- Bot restarted MySQL-only ✅

### Post-Migration Cleanup
- gspread + google-auth removed from requirements.txt
- `_SHEETS_TIMEOUT` → `_DB_TIMEOUT` renamed
- `service_account.json` removed
- mysqldump cron added
- All 7 cleanup actions done

### Key Files
- `/root/ACM-Personal-Wallet/bot/main.py` (5,197 lines)
- `/root/ACM-Personal-Wallet/bot/db.py` (437 lines)
- Verification: `/root/.openclaw/workspace/memory/projects/acm_wallet/migration-verification-2026-06-25.md`

---

## 2. PS VIBE Web — Staff Role → Members Tab Permission ✅

### Changes
| File | Change |
|------|--------|
| `dashboard-dist/assets/index-B6C8MOLE.js` | Router: `role:"admin"` → `roles:["admin","staff"]` |
| Same file | Sidebar `ie` array: added `"/members"` |

### Result
Staff role can now see and access Members tab in web dashboard.

---

## 3. 🐛 Customer Bot — Phone Last-3-Digits Member Lookup Fix ✅

### Bug
Customer Booking flow: "Member Card ရှိတယ်" → ဖုန်းနောက်ဆုံး ၃ လုံးရိုက် → **member ရှာမတွေ့**

### Root Cause
After MySQL migration, `api_fetch_members` endpoint now returns **full member objects** (`[{id, name, phone, ...}]`) instead of **flat ID list** (`["PSV-001", ...]`). Customer Bot's `_fetch_members()` treated dict objects as string IDs → invalid API URLs → silent failure → empty member list.

### Fix (2 files)
| File | Change |
|------|--------|
| `customer_bot/api.py:297-372` | `_fetch_members()` — detect format: if first item is dict → build members directly (1 API call instead of 1+N) |
| `customer_bot/booking_handlers.py:1160` | Balance lookup: `wallet_mins` → `balance_mins` → `balance` fallback chain |

### Bonus
Member fetch speed improved: 1 API call instead of 1 list + 7 detail calls.

---

## 4. PS VIBE — Data Digest System ✅
- `digest_engine.py` + `digest_routes.py` + `digest_cron.sh` built
- Registered in app.py, daily cron at midnight MMT

---

## Key Files Modified
| File | What |
|------|------|
| `/root/psvibe-sales-bot/customer_bot/api.py` | `_fetch_members()` fix |
| `/root/psvibe-sales-bot/customer_bot/booking_handlers.py` | Balance key fix |
| `/root/psvibe_api_server/dashboard-dist/assets/index-B6C8MOLE.js` | Staff permission |
| `/root/ACM-Personal-Wallet/bot/main.py` | Full MySQL migration |
| `/root/ACM-Personal-Wallet/bot/db.py` | MySQL layer |

---

## 5. 🔍 C06 — "Session End မနှိပ်ဘဲ Free" Investigation

### Boss Report
C-06 console became Free without staff pressing "End Session" button.

### Verdict: Manual End — NOT Auto-End ✅
Sale Bot logs confirm full manual end-session flow ran:
- `08:45:32 UTC` (15:15 MMT): `end_booking` called via Console Management → Session End
- `after_voucher_msg`, `after_delete_game`, `food_cart_lookup` all ran — full manual flow
- Staff member DID end the session through the bot (possibly without realizing)

### Evidence
- `session_timer.py`: Only sends reminders, does NOT auto-end
- `booking_flow.py _remind_loop`: Text explicitly says "Session auto-end မဖြစ်ပါ"
- API auto-cancel: Disabled since June 22

---

## 6. 🐛 C05 — Booking Creation Overwrites Active Console Status ⚠️ (NOT YET FIXED)

### Boss Report
C-05 showing as Free (actually "Reserved" in DB) while session was still Active.

### Root Cause Found — `POST /api/bookings` at app.py line ~2845
When a new CONFIRMED booking starts within 60 minutes, the endpoint **directly overwrites** `console_status` to `Reserved` WITHOUT checking if there's an Active session:

```python
# app.py line ~2845 — THE BUG
if start_dt <= now_mmt() + timedelta(minutes=60):
    cur.execute(
        "UPDATE console_status SET status='Reserved', current_member=%s, start_time=NULL WHERE console_id=%s",
        (bk_member_id or customer_name, console_id)
    )
```

### Timeline
| Time (MMT) | Event |
|------------|-------|
| 12:38 | C-05 Free → Reserved (booking #961 for 13:45) |
| 13:45 | C-05 Reserved → Active (R gyi, 120 min, ends 15:45) |
| **15:18** | **Booking #975 created** (Sai Aung Hein Ko, 16:00-18:00) → **Active overwritten to Reserved** |
| 15:35 (now) | C-05 still Reserved, booking #961 still Active in DB — status drift |

### Fix Plan
Replace direct `UPDATE console_status SET status='Reserved'` with `_sync_console_status(console_id)` which properly checks for Active bookings first:
1. Active booking found → keep Active ✅
2. No Active + confirmed within 60 min → Reserved ✅
3. Neither → Free ✅

### Affected Code
- `/root/psvibe_api_server/app.py`, line ~2845 (`POST /api/bookings`, customer bot booking creation)
- Same pattern may exist in staff booking creation path — need to audit

---

## 7. SEL Currency Exchange — Project Built ✅
- Full project under `/opt/kora-projects/sel_exchange/code/`
- SQLite DB, FastAPI (port 8001), Telegram bot with raw HTTP polling
- 15 API endpoints, FIFO P&L system, bot menu fully working
- Services: `sel-exchange-api` + `sel-exchange-bot` both active
- Awaiting Boss's payment account names before transactions can start

## Lessons Learned
- **#26: API format changes after migration** — When API returns different format, check ALL consumers
- **#27: Minified JS edits** — Use Python string replace for precision, not sed; always backup first
- **#28: Booking creation must respect Active sessions** — Direct `UPDATE console_status` in booking creation bypasses `_sync_console_status()` safety checks; always use the sync function for status derivation

---

## 8. Booking #974 Disappearance — Root Cause & Fixes ✅ (17:00 MMT)

### Investigation Result
Staff Booking flow on C-03 → `step_book_checkin_bind` called **`POST /api/update_booking`** (endpoint doesn't exist → HTTP 405). But `_api_call_async` catches HTTPError and returns dict instead of raising → code thought checkin succeeded (logged success) but booking was NOT updated to Active. Later `_sync_console_status` ran, found no Active booking → set C-03 Free → booking cancelled.

### Fix 1: Dead API Endpoint (HTTP 405)
- **File:** `/root/psvibe-sales-bot/bot/handlers/booking.py` line 1213
- **Before:** `_psvibe_post_async("update_booking", {"id": bk_id, "status": "Active", "console_id": cid})`
- **After:** `_psvibe_patch_async(f"bookings/{bk_id}/status", {"status": "Active", "consoleId": cid})`
- Service: `psvibe-sale-bot` restarted → PID 2294683

### Fix 2: _sync_console_status — NOW() (UTC) → now_mmt()
- **File:** `/root/psvibe_api_server/app.py` lines 96-158
- **Before:** SQL used `NOW()` (MySQL server UTC) to compare against `start_time` (stored as MMT) → ~6.5hr mismatch
- **After:** Generate `_now_mmt_str` and `_now_plus_60` via Python `now_mmt()`, use parameterized `%s`
- Also fixed `DATE_ADD(NOW(), INTERVAL 60 MINUTE)` and `end_time >= NOW()` clauses

### Fix 3: Booking → Active: start_time NOW() → MMT
- **File:** `/root/psvibe_api_server/app.py` line 1623
- **Before:** `start_time=NOW()` in `PATCH /api/bookings/{id}/status` when status→Active
- **After:** `start_time=%s` with `now_mmt().strftime('%Y-%m-%d %H:%M:%S')`
- Service: `psvibe-api` restarted → PID 2294664

### New Lessons
- **#29: HTTP error handling asymmetry** — `_api_call_async` returns error dict for 4xx (no exception) but `_psvibe_post_async` catches Exception → callers never know API failed. Check if response has `status_code` key.
- **#30: MySQL NOW() is UTC** — All time comparisons in SQL must use app-level MMT time passed as parameter. Direct `NOW()` in queries against MMT-stored datetimes will ALWAYS be wrong.
- **#31: Minified Vue SPA fixes must cover BOTH save logic AND template render** — Removing a `v()` condition from save function (`I()`) is not enough; the template ternary `v() ? ... : y("",!0)` also gates the INPUT fields. Miss this = fields invisible, save sends nothing.

### Fix 4-5: Dashboard Edit Booking — Template v() Removal
- **File:** `/root/psvibe_api_server/dashboard-dist/assets/BookingsManagement-BbqxMmPA.js`
- **Fix 4:** `,if(o.value.booking_date...` → `,o.value.booking_date...` (stray `if` after removing `v()){` wrapper)
- **Fix 5:** `v()?(d(),i(T,{key:0},[...time/duration fields...],64)):y("",!0)` → `(d(),i(T,{key:0},[...time/duration fields...],64))` (template always renders fields)
