# Daily Log — 2026-06-30

## SEL Currency Exchange — Contact Edit + Duplicate Check (08:59 - 09:03 UTC)

### Changes
1. **db.py** — Added `counterparty_update(cid, name, phone, notes, cp_type)` and `counterparty_get_by_name(name)` functions
2. **app.py** — Added `CounterpartyUpdate` model, `PATCH /api/counterparties/{cid}` endpoint, duplicate name check on POST and PATCH
3. **dashboard/index.html** — Added Edit button (✏️) to contact rows, `editContact()` modal function, `esc()` helper, save handler for `counterparty-edit`

### Bug Fixed During Implementation
- `execute()` in db.py returns `lastrowid` (0 for UPDATEs), causing `counterparty_update` to always return False
- Fixed by using `get_conn()` directly with `cur.rowcount > 0` instead of `execute()`

### Tests Passed
- ✅ PATCH existing contact → Updated
- ✅ PATCH non-existent → 404
- ✅ POST duplicate name → 409
- ✅ PATCH to duplicate name → 409
- ✅ Record correctly persisted in DB

### Files Modified
- `/opt/kora-projects/sel_exchange/code/db.py` (lines ~177-189)
- `/opt/kora-projects/sel_exchange/code/app.py` (lines ~154-222)
- `/opt/kora-projects/sel_exchange/dashboard/index.html` (multiple sections)
- `memory/projects/sel_exchange/state.md` (updated)
