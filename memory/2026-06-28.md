# 2026-06-28 Daily Memory

## Session: Docs Update (~02:03 UTC / 08:33 MMT)

### Boss Request
"Docs update တွေလုပ်ပါ" — comprehensive documentation refresh

### Actions Taken

#### MEMORY.md Updates
- Updated project count: 8 → 9 (added `sel_exchange`)
- Added missing critical lessons #28-38 to appropriate sections
- Added June 26-27 summary sections
- Updated Known Issues table — cleared resolved items, noted VPS unreachable

#### Daily Memory
- Created `2026-06-28.md` (this file)

#### Project State Files
- Checked all 9 project state files exist
- Updated timestamps

#### Auto Doc Updater
- Ran for all 9 projects

### Current Known Issues
| Issue | Severity | Status |
|-------|----------|--------|
| Cashflow month filter not applied | Medium | 🔴 Pending (Jun 26) |
| Cashflow asset deduction double-count | Medium | 🔴 Pending (Jun 26) |
| VPS (5.223.81.16) health monitor unreachable | Low | 🟡 Investigating |

### Files Modified
- `/root/.openclaw/workspace/MEMORY.md` — major update
- `/root/.openclaw/workspace/memory/2026-06-28.md` — created
- `/root/.openclaw/workspace/memory/projects/*/state.md` — timestamps

## Session: Ko Khant Salary Email (~02:31 UTC / 09:01 MMT)

### Boss Request
Ko Khant ရဲ့ email — rayleighdarking2004@gmail.com — salary structure ပို့ပေးရန်

### Actions
- Updated staff_records.email for Ko Khant in MySQL
- Re-sent salary structure email using correct format (matching Eaindray & Min Myat Naing template)
- Message ID: 19f0c156dd04a434
- Settings: base 400,000 + food_allow 200,000 + att_bonus 50,000 + food_com 5% - advance 250,000
- Leave: 0 days → Full attendance bonus

### Lesson
- ALWAYS mirror existing templates when sending repeating emails. Boss noticed the format mismatch immediately.

## Session: Fix Notification Targets (~03:07 UTC / 09:37 MMT)

### Boss Request
Stock reminder နဲ့ Analyst data တွေ Boss DM ကိုပဲ တိုက်ရိုက်ပို့နေတာ — admin group ထဲ ပြောင်းပို့ခိုင်းတယ်

### Investigation
Found 2 notification sources sending directly to Boss (6296803251):

| # | Service | Cron | Issue |
|---|---------|------|-------|
| 1 | Inventory Daily Summary | `0 2 * * *` (2AM UTC) | `alertBoss()` → Boss only |
| 2 | EOD Report | `30 13 * * *` (8PM MMT) | `BOSS_CHAT_ID` hardcoded |

Admin group chat ID: `-1003686032747` (already in inventory_alerts.js as STAFF_CHAT_ID)

### Fixes Applied

**1. Inventory Alerts** (`/opt/inventory_alerts/inventory_alerts.js`):
- `runDailySummary()`: `alertBoss(msg)` → `alertStaff(msg)`
- `alertStaff()` already has Boss fallback built-in

**2. EOD Report** (`/root/.openclaw/workspace/psvibe_eod_report.py`):
- Added `STAFF_CHAT_ID = "-1003686032747"`
- Changed send logic: try admin group first → Boss fallback on fail
- Created symlink: `/root/psvibe-sales-bot/scripts/eod_report.py` → workspace (crontab path fix)

**Result:** Both now send to Admin Group (-1003686032747) with Boss DM (6296803251) as fallback

## Session: Morning Snapshot Fix (~03:15 UTC / 09:45 MMT)

### Boss Request
Morning snapshot က today data နဲ့ပို့နေတယ် — မနက်အစောကြီးဆိုင်မဖွင့်ခင် sale 0 ဖြစ်နေတယ်။ မနေ့က data ပြသင့်တယ်။

### Investigation
Morning snapshot source: `daily-bi-report` cron → `biz_intel.js full` at 9:30 AM MMT
- `dailyReport()` was using `CURDATE()` (today) for ALL 4 queries
- At 9:30 AM, shop just opened = sales are 0
- Other morning reports already correct: `daily_sales_summary.py` uses `CURDATE()-INTERVAL 1 DAY`

### Fix Applied
**`/root/.openclaw/workspace/biz_intel.js`**:
- All 4 SQL queries: `CURDATE()` → `CURDATE() - INTERVAL 1 DAY`
- Date display: `today` → `yesterday` variable
- Report label: "Sales Overview" → "Sales Overview (Yesterday)"

## Session: Sale Bot Food Markdown Bug + Web Booking Notifications (~11:00 UTC / 17:30 MMT)

### Boss Request
"Review screen error" after Cash payment — what's happening?

### Root Cause
Same Markdown underscore bug in 2 more places. Food item `လျှာဂျွမ်းထိုး_ဟော့ဟော့` had `_` interpreted as Markdown italic, breaking `parse_mode="Markdown"`:
1. `_show_payment_review()` line 1220 — Review Screen (the error Boss saw)
2. `step_sale_confirm` line 1367 — Receipt

### Fix Applied
**`/root/psvibe-sales-bot/bot/handlers/sales.py`**:
- `_show_payment_review()`: `i['name']` → `_md_escape(i['name'])` in food_lines_review
- `step_sale_confirm`: `i['name']` → `_md_escape(i['name'])` in food_lines_receipt
- Now ALL 3 screens (Menu + Review + Receipt) use `_md_escape()`

---

### Boss Follow-up: Web dashboard booking confirm/cancel noti not arriving
"Web က နေ booking confirm / cancel က noti မရောက်တော့ ပြန်ဘူး — Code တွေ က ဖျက်မိနေတာလား"

### Investigation
- Sales bot uses `PATCH /api/bookings/{id}/status` (app.py line 1705) → HAS notification
- Web dashboard uses `PUT /api/dashboard/bookings/{id}` (dashboard_routes.py line 294) → NO notification
- My edits only touched food menu lines in sales.py — did NOT delete notification code

### Fix Applied
**`/root/psvibe_api_server/dashboard_routes.py`** — `PUT /bookings/{id}` endpoint:
- Added notification code (copied pattern from app.py PATCH endpoint)
- When status changes to `confirmed` → customer gets confirmation message
- When status changes to `cancelled` → customer gets cancellation message
- Uses `_send_telegram_customer()` via threading.Thread (fire-and-forget)
- `BOT_TOKEN` → `CUSTOMER_BOT_TOKEN` (correct token for customer-facing messages)
- Restarted psvibe-api service

---

## Session: Customer Bot Block + EOD Report Fix (~11:30 UTC / 18:00 MMT)

### Boss Request 1: Block BK#1121 & BK#1122 customers
Block telegram IDs `7158675982` (Unoman66) and `8383666570` (Phone 09691339153) from customer bot.

### Implementation
**`/root/psvibe-sales-bot/customer_bot/main.py`**:
- Added `BLOCKED_IDS` set + `BlockedUsersFilter` class
- MessageHandler with blocked filter (group=-1) — catches ALL text messages
- Block message: "🚫 Access Denied — အသုံးပြုခွင့် ပိတ်ထားပါသည် — Contact Admin: @psvibeofficial"

**`/root/psvibe-sales-bot/customer_bot/handlers.py`**:
- Added `_is_blocked()` check function
- Check in `cmd_start`, `cmd_menu`, `handle_menu_buttons`
- Block message returned directly (not silent drop)

### Boss Follow-up: Admin contact → @psvibeofficial
Changed all block messages from `@kingkong00787` → `@psvibeofficial` (4 locations total)

---

### Boss Request 2: EOD Report fix — data all zeros + change time to 10 PM
"PS VIBE Admin ထဲ ပို့တဲ့ report က data မှားနေတယ် 8pm MMT အစား 10pm နဲ့ ပြောင်းပေးပါ"

### Investigation
**Cron**: `30 13 * * *` → 13:30 UTC = 20:00 MMT (8 PM MMT)
**Script**: `/root/psvibe-sales-bot/scripts/eod_report.py`
**Bug**: Used `docker exec psvibe-mysql mysql` → auth failed (`Access denied for 'psvibe_user'@'127.0.0.1'`) → ALL queries returned None → all zeros in report

### Fix Applied (Complete Rewrite)
**`/root/psvibe-sales-bot/scripts/eod_report.py`**:
- Replaced `docker exec mysql` with `pymysql` direct connect
- All 9 queries now return real data (test confirmed: 42 txns, 703,500 Ks)
- Uses DictCursor for cleaner row access
- Added proper logging

### Cron Update
```
Before: 30 13 * * *  (8:00 PM MMT)
After:  30 15 * * *  (10:00 PM MMT)
```

### Test Results
- Script ran manually, sent to Admin Group successfully
- Data: 42 txns, 7.0 သိန်း, 3/10 consoles active, 2 members

### Files Modified
- `/root/psvibe-sales-bot/bot/handlers/sales.py` — `_md_escape()` in 2 more places
- `/root/psvibe_api_server/dashboard_routes.py` — booking notification code added
- `/root/psvibe-sales-bot/customer_bot/main.py` — block system + filter
- `/root/psvibe-sales-bot/customer_bot/handlers.py` — `_is_blocked()` checks
- `/root/psvibe-sales-bot/scripts/eod_report.py` — complete rewrite (pymysql)

### Services Restarted
- `psvibe-sale-bot` ✅
- `psvibe-customer-bot` ✅
- `psvibe-api` ✅

## Session: EOD Report English + Payment Fix (~14:43 UTC / 21:13 MMT)

### Boss Request 1: English language only
"Eng Language နဲ့ပဲ့ ပြန်ပြင်ရေးပေးလိုက်ပါ"

### Action
Converted entire Burmese report block to English:
- Title: "ယနေ့ အရောင်းအစီရင်ခံစာ" → "Daily Sales Report"
- All section headers and labels → English
- Number formatting: "သိန်း" → "L" (e.g., 7.0L Ks)
- Date format: "28 ဇွန် 2026 (တနင်္ဂနွေ)" → "28 June 2026 (Sunday)"
- Removed mm_months/mm_days dicts (no longer needed)

### Boss Request 2: Payment methods wrong
"Payment methods ကမှားနေသလိုပဲ့"

### Root Cause
`GROUP BY payment_method` grouped by full string like `Cash:20000`, `KPay:30000` instead of method name → 30+ separate line items instead of 3

### Fix
- Query: `GROUP BY SUBSTRING_INDEX(payment_method, ':', 1)` → extracts method name
- Also fixed: `members` table → `member_wallets` table (1 vs 8 records)
- Result: KPay: 3.9L, Cash: 3.6L, AYA Pay: 14K (cleanly grouped)

### Docs Update
- MEMORY.md: Added session summary + 5 new lessons (#51-#55)
- Daily memory: This section appended
- Auto-doc-updater: ran (entry #167)
