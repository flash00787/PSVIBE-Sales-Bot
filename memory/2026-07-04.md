# Memory Index (2026-07-04)

*Kora Memory v2 — Full content in MongoDB | Query: `kora_memory.py`*

## BUG (2)

- `[open [low]]` **Prediction: 🌅 **Kora Morning Briefing** — 2026-07-04 (စနေနေ့)
━━━━━━━━━━━━━━━━━━**  
  *auto-bug* | `id:8a8cc91b`
  > **Bug detected:** Prediction: 🌅 **Kora Morning Briefing** — 2026-07-04 (စနေနေ့) ━━━━━━━━━━━━━━━━━━━━━━

- `[open [low]]` **Prediction: 🌅 **Kora Morning Briefing** — 2026-07-04 (စနေနေ့)
━━━━━━━━━━━━━━━━━━**  
  *auto-bug* | `id:38526a80`
  > **Bug detected:** Prediction: 🌅 **Kora Morning Briefing** — 2026-07-04 (စနေနေ့) ━━━━━━━━━━━━━━━━━━━━━━

## FIX (1)

- `[fixed [critical]]` **SSD Return — Game Auto Re-add to Source SSD**  
  *ssd, console, games, return, bugfix* | `id:c756a297`
  > step_ssd_ret_game: SSD Return လုပ်တဲ့အခါ game ကို console မှဖျက်ပြီး source SSD (notes field: From Samsung T1 Shield → SSD-T1) ထဲ auto ပြန်ထည့်ပေးမယ်။ Previously game tracking ထဲက လုံးဝပျောက်သွားတာ။ A

## 2026-07-04 Session — AKT Clothing Shop ERP Fixes

### Critical Fix: Account Balance (Acc Bal) — payment_transactions Not Recorded 🐛→✅
**Time:** ~11:00-11:45 UTC (17:30-18:15 MMT)

**Root Cause:** `record_payment_transaction()` in `payment_accounts.py` looked up account by name, but payment method names didn't match account names:
- "Cash" → looking for "Cash" but account is "Cash Register"
- "Bank Transfer" → looking for "Bank Transfer" but account is "Bank Account"

**Fix:**
1. Added name mapping in `record_payment_transaction()`: `{"Cash": "Cash Register", "Bank Transfer": "Bank Account"}`
2. Added type fallback (cash/mobile_wallet/bank) when name lookup fails
3. Added `@field_validator` for empty string → None conversion in ProductCreate model
4. Backfilled Purchase #1 (GRN-20260704-0001): Cash -20,000 + KPay -20,000

**Files Modified:**
- `/opt/kora-projects/akt_clothing/code/backend/app/routes/payment_accounts.py` — name mapping + type fallback
- `/opt/kora-projects/akt_clothing/code/backend/app/routes/product_routes.py` — `@field_validator` for empty strings
- `/opt/kora-projects/akt_clothing/code/backend/app/routes/sale_routes.py` — credit payment records payment_transactions
- `/opt/kora-projects/akt_clothing/code/backend/app/routes/purchase_routes.py` — purchase records payment_transactions

**Verified Balances (all match ✅):**
| Account | Balance | 
|---------|---------|
| Cash Register | 987,000 Ks |
| KPay | 2,080,000 Ks |
| WavePay | 0 Ks |
| Bank Account | 0 Ks |

### Fix: Add Product — category_id/barcode Empty String Crash 🐛→✅
**Root Cause:** Pydantic `Optional[int]` rejected empty string `""` before reaching function body.

**Fix:** Added `@field_validator("category_id", "barcode", "brand", mode="before")` to ProductCreate model — converts `""` → `None`.

### UX Improvements — Sale Customer + UI Clarity
- Sale form: Customer field marked "(optional)" with "🚶 Walk-in Customer" default
- Add Product form: Required fields marked with red `*`
- Buttons: "+ Add Product" / "+ New Sale" enlarged for mobile
- Search: placeholder updated to "Search name, code, or barcode..."

### Code Graph Refresh
- MongoDB code graph rebuilt: 7,858 entities, 103K relations
- Full rebuild completed for weekly maintenance
